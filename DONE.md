# DONE Mandarine

Historial de lo completado. El roadmap activo vive en `TODO.md`.

- **GEO SEO + AEO: landing pública pre-auth + metas/estructura para answer engines (2026-08-15)** — item del roadmap con llmaudit score 22 ("likely invisible"): la SPA era 100% login-gated y la superficie pública era solo `index.html` (meta description básica) + `robots.txt` minimalista. Decisiones del usuario: **landing en la SPA** (estilo y deploy unificados, en vez de página estática) y **sumar `llms.txt`**. (1) **`index.html`**: title/description expandidos ("qué es / para quién"), `canonical` → `https://mandarina-fi.vercel.app/`, Open Graph (`og:type/site_name/title/description/url/image` + `og:locale es_AR`), Twitter card `summary_large_image`, y **JSON-LD `@graph`** con `Organization` (logo), `SoftwareApplication` (Finance, `offers` precio 0) y **`FAQPage` con las 3 preguntas del reporte en ES + EN** ("what is mandarina fi and who is it for", "why choose mandarina fi", "what to expect") — respuestas estáticas en HTML, crawlables sin JS. (2) **`components/Landing.js` (nuevo)**: página pública pre-auth con header de marca, hero (h1 "A tu plata, sacale todo el jugo" + párrafo qué es/para quién), sección "Por qué Mandarina" (3 features), **FAQ con `<details>`/`<summary>`** (mismo contenido que el JSON-LD, texto visible en el DOM para crawlers con render JS) y el **card de `Auth` embebido** al final; `App.js` ahora renderiza `<Landing>` sin sesión (`Auth` pelado eliminado como página pública). (3) **`Auth.js`**: prop `embedded` — omite el wrapper `min-h-screen`, los blobs, el `ThemeToggle` y el bloque de branding para reusarse dentro de la landing (default sin cambios; `Auth.test.js` intacto). (4) **`public/robots.txt`**: grupos explícitos `Allow: /` para GPTBot, OAI-SearchBot, ClaudeBot, PerplexityBot y Google-Extended + `Sitemap:` line. (5) **`public/sitemap.xml`** (URL raíz, weekly, priority 1.0) y **`public/llms.txt`** (descripción + FAQ en markdown para LLMs). **Verificado post-deploy**: `/` 200 con JSON-LD `FAQPage`, `og:title` y `canonical` en el HTML servido; `/robots.txt`, `/sitemap.xml` y `/llms.txt` servidos. Suite **337/337** (+4 en `Landing.test.js`) + lint 0 + build prod OK. **Pendiente**: re-correr llmaudit en el navegador (el re-audit es un form live contra Gemini; no hay API programática) para confirmar la mejora del score.

- **Fix móvil del módulo Operaciones — recuadros de resumen que no entraban (2026-08-15)** — feedback del usuario: en versión mobile, el último módulo de Inversiones (Operaciones, ledger) **no desplegaba bien los valores en sus recuadros**. Causa: `LedgerView.js` forzaba `grid grid-cols-3 gap-3` en el panel de resumen (Invertido / Valor actual / Ganancia-Pérdida) → en pantallas angostas cada card quedaba ~110px y montos como `$1.234.567,89` en `text-lg` (más el badge `▲ x%` inline en Ganancia/Pérdida) desbordaban sus recuadros. **Fix (1 línea, `LedgerView.js:207`): `grid grid-cols-1 gap-3 sm:grid-cols-3`** — en `<640px` las 3 cards se apilan a ancho completo (mismo patrón responsive que `SummaryCards`: `grid-cols-1 sm:grid-cols-2 xl:grid-cols-4`); en `sm+` vuelve a las 3 columnas. Sin cambios de lógica ni backend. Suite **333/333** + lint 0 + build prod OK. Frontend-only (sin `db push` ni redeploy de functions). **En la misma sesión**: item **ONs/cauciones en la edge `quotes` → EN HOLD** por decisión del usuario ("no agreguemos cosas para mostrar datos vacíos"): verificado que de los 5 tickers del usuario (`PN360, TLCTO, TTC90, VSCRO, YM340`) solo **TLCTO/VSCRO** resuelven en BYMA Open Data (ONs, `settlementType:'2'`, la edge los parsea sin cambios) mientras **PN360/TTC90/YM340** no existen en el feed gratis (ni cotización con st 1/2/3/6/7 ni histórico) y las cauciones con símbolos estándar (`A7D`, `A30D`, `60D`…) tampoco → agregar los tipos solo habría mostrado datos vacíos. Detalle y condiciones de reactivación en `TODO.md`.

- **Migración a las nuevas API keys de Supabase (`sb_publishable_…`/`sb_secret_…`) (2026-08-14)** — Supabase **depreca las legacy keys** (`anon`/`service_role`) a fines de 2026 y **ya no se pueden rotar** (solo ver/copiar; verificado 2026-08-09); las nuevas keys sí son revocables individualmente. Decisiones: (a) la **publishable reemplaza `anon`** (client-side); (b) la **secret reemplaza `service_role`** pero **no se usa en el proyecto** (las Edge Functions autentican con el JWT del usuario vía `verify_jwt = true`, nunca `service_role`) → se guarda solo en el dashboard; (c) las functions **ya no necesitan key del usuario**: leen `SUPABASE_PUBLISHABLE_KEYS` (JSON auto-inyectado por la plataforma, key `default`), **sin fallback a la legacy** — el valor solo es el argumento obligatorio de `createClient`, la autorización real la da el JWT en `Authorization`. (1) **Dashboard**: creada la publishable `default` (`sb_publishable_…`) en Settings → API Keys (convive con las legacy; no rompe nada). (2) **Frontend**: `frontend/.env` → `VITE_SUPABASE_ANON_KEY` = valor publishable (mismo nombre de var, nuevo valor; `VITE_SUPABASE_URL` intacto) + **Vercel actualizado** vía CLI en **Production y Preview** (`vercel env rm VITE_SUPABASE_ANON_KEY` + `vercel env add … --value`); la env de Preview quedó "aplica a todos los branches" (branch vacío). (3) **Backend**: nuevo `_shared/supabase.ts` con `createUserClient(authHeader)` (parsea `SUPABASE_PUBLISHABLE_KEYS`, devuelve `keys['default']`, `''` si malformado o ausente — sin fallback a legacy), y `parse-summary`/`import-plan`/`quotes` pasan a usarlo (se elimina el `createClient` + `Deno.env.get('SUPABASE_ANON_KEY')` inline de los 3). Deploy: `supabase functions deploy parse-summary import-plan quotes` (sin migración DB → sin `db push`) + `vercel --prod` desde la raíz. Verificado: `deno test` 64/64, check/fmt/lint OK; bundle de prod con `sb_publishable_UpTGS1NZirI5KctsykNCbg_kat2Rb3-` y **sin** el JWT anon legacy (`eyJhbGci…` ausente); la edge `quotes` responde `OPTIONS 200` y `{"code":"UNAUTHORIZED_NO_AUTH_HEADER"}` sin JWT / `No autenticado` con la key en Bearer (correcto: exige JWT de usuario). **Pendiente del usuario**: desactivar las legacy keys (`anon`/`service_role`) en Settings → API Keys (reversible) tras confirmar la app en uso.

- **Ledger — "Registrar operación" en pop-up + BYMA siempre visible (2026-08-14)** — feedback del usuario: el form inline dejaba el botón Registrar solo en un 3er renglón y prefería el registro en un **pop-up** con nota grande y botón abajo de todo; la página principal debía quedar solo con datos. Decisiones: (a) **nuevo `components/RegisterOperationModal.js`** — modal portalado a `document.body` (patrón `SummaryDetailModal`: backdrop `fixed bg-black/50` + cierre por X/Esc/backdrop, `data-testid="register-modal-backdrop"`, `role="dialog"` `aria-label="Registrar operación"`); dentro, grilla `grid-cols-2 sm:grid-cols-4` con **Tipo · Ticker * · Fecha · Cantidad *** en el 1er renglón y **Precio+BYMA / Comisión+$|%** (a 2 columnas) abajo, **Nota como textarea grande** (`rows={4}`, ancho completo con `col-span-2 sm:col-span-4` — en el primer build el `col-span-2` la dejaba a media columna en desktop, corregido en el mismo feedback) y hint "Los campos con * son obligatorios · Precio en $ · Comisión en $ o %" + botón **Registrar como botón normal abajo a la derecha** (footer `flex flex-wrap items-center justify-between`: hint a la izquierda, botón `px-5 py-2` con "Registrando…" al registrar — ya no es `w-full`); el diálogo creció de `max-w-2xl`/`p-5` a **`max-w-3xl`/`p-6`**. Misma validación e insert de antes (`commission_is_pct`, `notes`), toast de éxito y cierre del modal + `onRegistered()`. El form se **resetea por remount con `key`** (el padre incrementa `formKey` al abrir) — evita el anti-patrón `setState` síncrono en effect que el lint `react-hooks/set-state-in-effect` rechazaba. (b) **BYMA siempre visible con fetch on click**: ya no depende del debounce de `draftSymbol`; al clickear valida el ticker (invalido → toast "Ticker inválido"), invoca la edge `quotes` con `{ symbols: [symbol] }` y aplica el precio (o toast "No se pudo obtener la cotización de X"); se eliminó el efecto del debounce 400ms, `quoteForDraft` y `draftSymbol` de los símbolos de `useWatchQuotes` → menos llamadas a la edge. (c) **`LedgerView.js`**: se saca el form inline y su estado (`form`/`saving`/`setField`/`handleAdd`/`inputClass` → viven en el modal) → la página principal queda con header (título, **botón primario "Registrar operación"** con ícono +, refresh, ARS/USD + CCL/MEP), cards de resumen, tabla por símbolo, listado de operaciones y el **estado vacío** que también abre el modal. (3) **Tests** (`LedgerView.test.js`): los del form reescritos con helper `openModal` (abren el pop-up antes de llenar); nuevos: **BYMA on-click** (trae el precio live y llama a la edge con el símbolo / ticker inválido avisa y NO consulta / sin cotización → toast) y **cierre del modal** por Escape, X y backdrop (mismo test) + cierre tras registrar. Suite **333/333** (+3) + lint 0 + coverage lib 96.5% / hooks 97.9% (meta ≥80%) + build prod OK. Frontend-only → **sin `db push`** (la edge `quotes` ya existe). Deploy: push a `master` → autodeploy Vercel, verificado por bundle.
- **Ledger: comisión en % o fijo + nota visible + campos obligatorios (2026-08-14)** — feedback del usuario en testing del ledger: (a) la comisión era **monto fijo en ARS** sin que se notara la unidad, (b) la **nota opcional se guardaba en la DB pero no se mostraba** en la tabla de operaciones, (c) los campos obligatorios no estaban marcados. Decisiones del usuario: **soportar % y fijo**, **columna Nota**, **marcar obligatorios** (sin exigir precio en ventas). (1) **Backend — `0017_ledger_commission_pct.sql`**: `add column commission_is_pct boolean not null default false` (filas existentes → fijo). **Aplicada en prod con `supabase db push` ANTES del push**. (2) **Frontend — `lib/ledger.js`**: nuevo `commissionAmount(op)` (si `commission_is_pct` → `qty × price × %/100`; si no, o si el flag falta — filas legacy — → monto fijo) usado en `costBasis` y en el `signedAmount` de la UI; `LedgerView.js`: toggle segmentado **$ | %** junto al campo Comisión (default `$`, el insert manda `commission_is_pct`), la celda de Comisión de la tabla muestra **`X%`** para las operaciones porcentuales (sino el monto en la moneda del toggle), nueva **columna "Nota"** en la tabla de operaciones (texto truncado con `max-w` + `title` = nota completa, "—" si no hay), y `required` + placeholder **`Ticker *`**/**`Cantidad *`** + hint "Los campos con * son obligatorios · Precio en $ · Comisión en $ o %" (la validación custom de toasts se mantiene y complementa al `required` nativo). (3) **Tests**: `lib/ledger.test.js` +3 (`commissionAmount`: fijo/legacy, porcentaje, ausente) +1 (`costBasis` con comisiones %), `LedgerView.test.js` +2 (insert con `commission_is_pct: true` al togglear a %; columna Nota truncada con tooltip + `0.6%` y "—" en la fila sin nota) y el insert esperado del test de compra actualizado con `commission_is_pct: false`. Suite **330/330** (+6) + lint 0 + coverage lib 96.5% / hooks 97.9% (meta ≥80%) + build prod OK. Deploy: `supabase db push` (0017) ANTES del push a `master`; sin cambios en las Edge Functions (sin redeploy).
- **Ledger de operaciones del Plan — tab "Operaciones" (2026-08-14)** — segundo item de la cola "Backend / deferido". **Reframe del usuario**: "el plan es una cosa, las ganancias que hiciste otra" → el ledger es un **tab independiente**, sin acoplar a `portfolio_plan` (se descartó derivar `quantity` del ledger; el plan y sus 3 vías de escritura quedan intactos). Decisiones cerradas en planning: tipos **compra/venta/ajuste**, costo **promedio móvil** con comisiones, posición inicial (ajuste) con **precio BYMA actual** vía prefill en el form, y el botón **comprar** del presupuesto **también registra la operación**. (1) **Backend — `0016_ledger.sql`**: tabla `public.ledger_operations` (`id uuid pk`, `user_id → auth.users on delete cascade`, `symbol`, `side` check `in ('compra','venta','ajuste')`, `quantity > 0`, `price >= 0`, `commission >= 0 default 0`, `currency default 'ARS'`, `date date`, `notes`, `created_at/updated_at` + trigger `set_updated_at`), índice `(user_id, symbol, date)`, RLS select/insert/update/delete own y comments — espejo de `0015`. **Aplicada en prod con `supabase db push` ANTES del push** (CRUD sin PGRST202). Sin cambios a `portfolio_plan`, RPCs, `import-plan` ni edge `quotes` (sin redeploy de functions). (2) **Frontend**: `lib/ledger.js` (puras `ledgerQuantity` = Σ compra+ajuste − venta; `costBasis` = **promedio móvil** ordenado por fecha — compras/ajustes suben el costo ponderado `(avg·prevQty + qty·price + commission)/newQty`, ventas solo bajan cantidad sin tocar `avgCost`; `profitability` = `(precio − avgCost)·qty` + `pnlPct`; `summarize` agrupa por símbolo y ordena), `components/LedgerView.js` (nuevo, renderizado en el tab **Operaciones** de `InvestmentsView.js`): form de alta (tipo/ticker/fecha/cantidad/precio/comisión/nota) con validación de ticker (`normalizeSymbol`/`validateSymbol`) y cantidad > 0, botón **BYMA** que precarga el precio con la cotización live (el ticker del form se suma a `useWatchQuotes` con **debounce de 400ms** para no spamear la edge al tipear, y así el prefill funciona para símbolos nuevos), panel resumen **Invertido / Valor actual / Ganancia-Pérdida** con % ▲▼, tabla por símbolo (cantidad, costo prom., precio, rentabilidad coloreada), tabla de operaciones (subtotal firmado según tipo, baja con confirmación inline "¿Borrar?"), toggle ARS/USD + selector CCL/MEP **compartidos**, `QuotesErrorNotice` + refresco, estados vacío/skeleton/error y toasts amigables — patrón Watchlist. **`InvestmentPlan.applyBuy`** además del `update quantity` inserta una op `compra` en el ledger (`price = amount/qty`, `currency 'ARS'`, `date` hoy, `notes 'Compra por presupuesto'`). (3) **Tests**: `lib/ledger.test.js` (13: promedio móvil con comisiones, ventas sin tocar `avgCost`, ajuste inicial, orden cronológico, venta de más, sin ops), `components/LedgerView.test.js` (12: resumen + rentabilidad, subtotales por tipo, alta compra/ajuste, prefill BYMA, validaciones, baja con confirmación, error de carga, USD con rate, error de quotes), smoke del tab en `InvestmentsView.test.js` + assert del insert en `InvestmentPlan.test.js`. Suite **324/324** (+26) + lint 0 + coverage lib 96.5% / hooks 97.9% (meta ≥80%) + build prod OK. **Mandi** leerá `ledger_operations` a futuro ("¿cuánto ganaste con GGAL?").
- **Fix header desktop: branding pegado al botón de guía (2026-08-14)** — regresión de QW-G (drawer móvil, `251d70f`): el header agregó un grupo móvil (`flex min-w-0 flex-1 items-center gap-2`) que contiene la hamburguesa "Abrir menú" y el título de vista — su contenido es `lg:hidden`, **pero el contenedor no**, así que en desktop quedaba vacío con `flex-1` absorbiendo todo el ancho y **empujaba el branding** (toggle del rail + logo "Mandarina" + tagline) contra el grupo de acciones (guía + tema + logout) a la derecha, en vez de alineado con el rail. **Fix (1 línea, `App.js`): `lg:hidden` en el grupo móvil** → en `lg+` el header queda con 2 hijos y `justify-between` restaura el layout (branding a la izquierda con el toggle alineado a x=0 vía `-ml-4 sm:-ml-6`, acciones a la derecha); en `<lg` nada cambia. Test de regresión estructural en `App.test.js` (el contenedor de "Abrir menú" conserva `flex-1` y lleva `lg:hidden` — jsdom no aplica CSS, así que se guarda la clase). Suite **298/298** (+1) + lint 0.
- **Watchlist: tickers a seguir fuera del plan (2026-08-14)** — primer item de la cola "Backend / deferido", con **persistencia en tabla DB** (decisión tomada: patrón `portfolio_plan`, RLS own por `user_id`; NO localStorage — ese precedente queda para preferencias de UI). (1) **Backend — `0015_watchlist.sql`**: tabla `public.watchlist` (id uuid pk, `user_id → auth.users on delete cascade`, `symbol`, `name`, `asset_type default 'otro'`, `currency default 'ARS'`, `sort_order`, `created_at/updated_at`), unique `(user_id, symbol)`, índice por `user_id`, RLS select/insert/update/delete own y comments — espejo de `0007`. **La edge `quotes` NO cambió** (ya resuelve símbolos arbitrarios vía BYMA) → sin redeploy de functions. (2) **Frontend**: `lib/watchlist.js` (puro: `normalizeSymbol` = upper sin espacios, `validateSymbol` = charset `[A-Z0-9.+=-]`, ≤12, no vacío), `hooks/useWatchQuotes.js` (invoca `quotes` con `{ symbols }`, devuelve `{ quotes, rates, refreshQuotes, quotesError }` sin `buildPlan`; los `rates` se guardan para respetar el toggle ARS/USD compartido), `components/Watchlist.js` (alta con input de ticker + etiqueta opcional, baja con confirmación inline "¿Quitar?", lista con precio + `change%` ▲▼ coloreado, refresco, reusa `QuotesErrorNotice`, estados vacío/skeleton/error, toasts amigables; escala ARS↔USD con CCL/MEP vía `display`/`rateMode`), y render en el tab **Cotizaciones** de `InvestmentsView.js` debajo de `MarketQuotes` con header propio ("Mi watchlist") — sin tocar `QuotesTable`/Plan. (3) **Tests**: `lib/watchlist.test.js` (7), `hooks/useWatchQuotes.test.js` (6), `components/Watchlist.test.js` (11, con `supabase.mockTable('watchlist', …)`), smoke en `InvestmentsView.test.js` (watchlist monta con el estado compartido ARS/USD + CCL). Suite **297/297** (+22) + lint 0 + coverage lib 96.4% / hooks 97.9% (meta ≥80%). Deploy: `supabase db push` (0015) **antes** del push a `master` (CRUD sin PGRST202) — la migración quedó en `0015` local+remota.
- **CI de GitHub en verde de nuevo (2026-08-14)** — el job `validate` de **Frontend** volvía en rojo desde el push de QW-E (`8923868`) por `npm audit --audit-level=high` (vuln alta **`nanoid <3.3.18`**, GHSA-2v37-7h3g-55p8, transitiva de `vite → postcss`). **Fix**: `npm audit fix` bumpó `nanoid` a **3.3.18** (dentro del rango `^3.3.16` de postcss, sin tocar vite) → `npm audit` 0 vulnerabilidades. **Warning** de Node 20 deprecado (checkout/setup-node corrían en Node 24): bump de `actions/checkout@v4`→**`@v7`** y `actions/setup-node@v4`→**`@v7`** en `frontend.yml` (y `checkout@v7` en `backend.yml`) — mismos PRs que dependabot ya había abierto. El `validate` de Backend seguía verde.

- **QW-H: fix botón de sesión duplicado + números que no entraban (2026-08-13)** — feedback del usuario tras QW-G. (1) **"Cerrar sesión" aparecía 2 veces en móvil**: el botón del header (`App.js`) no tenía clase responsive → se mostraba en `<lg` **y** el footer del drawer también lo tenía. **Fix**: `hidden lg:inline-flex` en el header → en móvil el logout queda **solo en el panel izquierdo (drawer)**, en desktop sigue en el header (el drawer es `lg:hidden`); el email del header (`sm:block`) no se tocó. (2) **Los números no entraban en las cards**: `SummaryCards` fijaba `text-2xl` + grilla `grid-cols-2 md:grid-cols-4` → en md–lg con el rail expandido (y en teléfonos con 2 cols) las columnas quedaban angostas y montos como `$1.234.567,89` desbordaban. **Fix (decisión del usuario: "2×2 hasta xl, 4 fila en xl")**: grilla no-compact pasa a `grid grid-cols-1 gap-3 sm:grid-cols-2 xl:grid-cols-4` — móvil <sm 1 columna (ancho completo), sm–lg 2×2 (cada card ~290px), xl+ 4 en fila (se mantiene `text-2xl`); el modal compacto (`grid-cols-2`) no cambia. Test responsive de `App.test.js` ajustado (selector `.grid.grid-cols-1` + `sm:grid-cols-2`/`xl:grid-cols-4` en vez de `md:grid-cols-4`). Suite **275/275** + lint 0 issues + coverage lib 96.3% / hooks 99.1% (meta ≥80%). **Con esto se cierra la cola frontend-only de los quick wins (QW-A..H).**
- **QW-G: drawer móvil custom (reemplaza la bottom nav) (2026-08-13)** — la navegación móvil pasó de la bottom nav (`<lg`, 2 items + logo central) a un **drawer deslizante LINDO** (decisión del usuario: no el default). Decisiones: **reemplaza** la bottom nav (no convive) y lleva **nav + email + Cerrar sesión** (los botones ayuda/tema siguen en el header móvil). (1) **`components/MobileDrawer.js` (nuevo)** — backdrop `fixed inset-0 z-40 bg-slate-950/40 backdrop-blur-sm animate-fade-in` (click → close) + panel `fixed inset-y-0 left-0 z-50 w-72 animate-slide-in-left` (keyframe ya existía) con rol `navigation aria-label="Navegación principal"` (reusa el aria de la bottom nav para los tests); header de branding (Logo + "Mandarina" + tagline, coherente con Auth/rail) + botón X; items `NAV_ITEMS` con el **mismo estilo active del rail** (`bg-brand-50 text-brand-700 ring-brand-600/20`) + `data-tour`, click → `onNavigate` + cierre; footer email truncate + "Cerrar sesión"; cierre con Escape. (2) **`App.js`** — estado `drawerOpen` (cierra al navegar/logout/goHome); hamburguesa en el header móvil (`aria-label="Abrir menú"`, mismo estilo que el toggle del rail); **eliminada la bottom nav** (`App.js` viejo 318-353) y el `<main>` volvió a `p-4 sm:p-6 lg:p-8 lg:pb-8` (sin el `pb-24` de reserva). (3) **Tests** (`App.test.js`) — bloque móvil reescrito: hamburguesa abre el drawer (2 items + email + Cerrar sesión), navegar a Resúmenes abre la pestaña de carga y cierra el drawer, cambiar de vista lo cierra, cerrar con X/backdrop/Escape, Cerrar sesión del footer → `signOut`; test `data-tour` ajustado (items leídos del drawer + rail). Edge case documentado: en mobile el tour no resalta los ítems de nav con el drawer cerrado (el tour ya saltea targets sin tamaño visible). Suite **275/275** (+3 neto) + lint 0 issues + coverage lib 96.3% / hooks 99.1% (meta ≥80%).
- **QW-F: flujo de cambio de contraseña (pantalla de nueva clave) (2026-08-13)** — el link de recuperación (`resetPasswordForEmail`) ya volvía a `window.location.origin` con sesión `PASSWORD_RECOVERY`, pero `App.js` no la manejaba → el usuario caía al dashboard sin setear la clave. Decisiones del usuario: **no duplicar reglas de seguridad** y **pantalla nueva separada**. (1) **`lib/password.js` (nuevo)** — centraliza `strengthOf`, `STRENGTH_LABEL`, `STRENGTH_BAR` y `validatePassword` (≥8 + `letters_digits`) que vivían en `Auth.js`; `Auth.js` pasó a importarlos con comportamiento idéntico (suite de Auth sin cambios). (2) **`components/NewPasswordScreen.js` (nuevo)** — misma estética que `Auth` (card, logo, ThemeToggle, blob, fondo gradient): "Nueva contraseña" con password + confirmar (toggle ojo compartido), barra de fuerza, errores inline `FieldError`/`role="alert"`; submit → `updateUser({ password })`; error → `authErrorToSpanish`; éxito → `signOut()` + toast "Contraseña actualizada". (3) **`App.js`** — estado `recovery`: `onAuthStateChange` setea `true` ante `PASSWORD_RECOVERY` y `false` ante `SIGNED_OUT`; el greeting de `SIGNED_IN` quedó intacto (no dispara en recovery); con `session && recovery` se renderiza `NewPasswordScreen` en vez del shell (el `getSession()` del boot cubre el link con reload). (4) **Mock** (`test/setup.js`) — `updateUser: vi.fn()` en `client.auth` (no existía). Tests: `NewPasswordScreen.test.js` (5: render, validación corta/sin letras-números, no coinciden, `updateUser` con la password + `signOut`, error traducido) + 2 integración en `App.test.js` (evento `PASSWORD_RECOVERY` → pantalla y NO dashboard/toast de saludo; flujo completo → `signOut` y vuelve al login). Suite **272/272** (+7) + lint 0 issues + coverage lib 96.3% / hooks 99.1% (meta ≥80%).
- **QW-E: cards de resumen 4 simétricas sin huecos (2026-08-13)** — feedback del usuario: las `SummaryCards` se armaban **condicionalmente** según hubiera USD → **Ingresos sin USD = 3 tarjetas** (hueco para 1) y **Egresos con USD = 5** (hueco para 3). Decisiones cerradas: **4 tarjetas simétricas** en ambos modos y **USD en 0 (total) / — (mayor)** cuando no hay. (1) **`SummaryCards.js`** — `cardsFromAnalysis`/`cardsFromIncome` construyen **siempre 4**: Egresos → `Débitos` (sub "N movimientos"), `Mayor gasto ARS` (sub comercio), `Gastos USD` (0 si no hay), `Mayor gasto USD` ("—" sin comercio si no hay); Ingresos → espejo (`Ingresos`, `Mayor ingreso ARS`, `Ingresos USD`, `Mayor ingreso USD`). La card "Movimientos" standalone se elimina (su conteo pasa al subtítulo de Débitos/Ingresos). Mayor sin datos: `value '—'` con `format: null` (el `Card` ya renderiza el value directo); totales: `0` con `fmt(USD)` → `$0.00`. **Mismo patrón defensivo 0/— para ARS** (aparece al filtrar Moneda: USD, donde `maxExpense`/`maxIncome` son `null`). Grilla intacta (`grid-cols-2 md:grid-cols-4`; compact `grid-cols-2`) → 4 tarjetas llenan la fila en dashboard, resumen y modal (2×2). (2) **Tests** (`Dashboard.test.js`) — señales de carga `'Movimientos'` → `'Débitos'`/`'Gastos USD'`; **nuevos**: siempre 4 cards (hijos de `summary-cards`) y USD en `$0.00`/`—` cuando no hay (egresos e ingresos, con queries acotadas al container por colisión de textos). Suite **265/265** (+2) + lint limpio + coverage lib 96.4% / hooks 99.1% (meta ≥80%).
- **QW-D: pulido de layout y toggle de filtros (2026-08-13)** — feedback del usuario tras QW-C: (1) la barra "Top orígenes/comercios" cubría **media columna** del grid de `SpendingCharts` (`lg:grid-cols-2` con 3 cards → fila 2 con celda derecha vacía = el "hueco") mientras `IncomeSources` y el Detalle eran full-width; (2) para volver de un click en "Acreditaciones por origen" había que limpiar el filtro. **Fix** (decisiones del usuario: barra full-width + toggle también en barras): (1) `SpendingCharts.js` — `lg:col-span-2` a la tarjeta de la barra → full-width en ambos modos, ritmo uniforme con Acreditaciones/Detalle; (2) `Dashboard.js` — helper `toggleMerchant(merchant)` (si el `query` ya es ese merchant case-insensitive → `''`, si no → merchant) usado en `onBar` del gráfico y en `onSelect` de `IncomeSources` → primer click filtra, segundo click restaura, igual que el doughnut con `focusCategory`. Tests: integración en `Dashboard.test.js` (click en un origen filtra a ese merchant; segundo click restaura) + polyfill `Element.prototype.scrollIntoView` en `setupTests.js` (jsdom no lo implementa → eran errores no capturados). Suite **263/263** (+1) + lint limpio + coverage lib 96.0% / hooks 99.1% (meta ≥80%).
- **QW-C Ingresos: subtotales y recurrencia (2026-08-13)** — la pestaña Ingresos ganó una sección **"Acreditaciones por origen"** que separa el **sueldo** (recurrente) de las **devoluciones** (puntuales) sin hardcodear merchants. (1) **Lógica** (`lib/analysis.js`): nueva `buildIncomeSources(txs, { minMonthsRecurring = 2 })` agrupa créditos (`amount > 0`) por merchant → `{ merchant, category, count, total, monthCount, recurring }`; `monthCount` = **meses distintos** desde el joined `card_summaries.period_year/period_month` (helper `monthKey`, con **fallback al mes de `tx.date`** para summaries viejos sin período); `recurring = monthCount >= 2`; ordenado por `total` desc y **sin cortar**. `buildIncomeAnalysis` ahora expone `result.sources`; el `byMerchant` top-10 de la barra del gráfico quedó intacto. (2) **UI** (`components/IncomeSources.js`, nuevo): tarjeta compacta con estilos de `SpendingCharts`, fila con merchant truncado, badge **"Recurrente"** (pill verde), subtítulo `categoría · ×N`, total en `fmt` (moneda nativa — no hay tipo de cambio por transacción, decisión del usuario), contador de orígenes, y **click en un origen filtra la tabla** (`setQuery` + `scrollToTable` en `Dashboard.js`, solo con `isIngresos`). Suite **262/262** (+11: 6 unit de `buildIncomeSources` + 4 de `IncomeSources` + integración en Dashboard) + lint limpio + coverage lib 96.0% / hooks 99.1%.
- **Fix del modal de resumen, segunda ronda (2026-08-13)** — el portal no alcanzó: el backdrop era `absolute inset-0` **hijo del contenedor que scrollea** (`overflow-y-auto`), así que al bajar el contenido alto (el `Dashboard` completo) el overlay se iba con el scroll y se veía el fondo de la app ("arriba negro, después se corta y vuelve el azul/blanco"). **Fix (estructura robusta e iOS-safe)**: backdrop `fixed inset-0` pegado al viewport (sus ancestros no tienen `overflow`/`transform`), scroll movido a un wrapper propio (`relative h-full overflow-y-auto`) y centrado vertical del diálogo (`flex min-h-full items-center justify-center`). **Aplicado también a `QuoteModal`** (por consistencia; antes `absolute` + `items-center` sin scroll). Test de regresión: `summary-modal-backdrop`/`quote-modal-backdrop` con clase `fixed` y sin `absolute`. Suite **251/251** (+1) + lint limpio + coverage lib 96.4% / hooks 99.1%.
- **Fix visual del modal de detalle de resumen (2026-08-13)** — bug reportado por el usuario tras los quick wins 2 (el modal se bugueaba con los fondos). **(1) Overlay cortado / "se ve el fondo azul"**: `SummaryDetailModal.js` renderizaba `position: fixed` ANIDADO bajo el div `.animate-fade-in-up` de `ResumenesView` (anima `transform` → crea un *containing block* para descendientes `fixed`) y dentro de `<main class="overflow-y-auto">` → el backdrop `bg-black/50` se cortaba a mitad de pantalla y se veía el fondo de la app. **Fix**: portalear el modal a `document.body` con `createPortal` (patrón ya usado en `Dropdown.js`; el keydown de Escape escucha en `window`, no se tocó). **(2) Números que no entraban en las cards**: `SummaryCards` usaba `grid-cols-2 md:grid-cols-4` con breakpoints por viewport → dentro del modal `max-w-3xl` las 4 columnas quedaban ~160px y los montos completos (`$1.234.567,89` en `text-2xl tabular-nums`) desbordaban. **Fix**: prop `compact` en `SummaryCards` → grilla fija de **2 columnas** (sin `md:grid-cols-4`, también en el skeleton); `Dashboard` recibe `compact` opcional y lo propaga; el modal lo setea; `min-w-0` en las cards para no desbordar el grid. `QuoteModal` usa el mismo patrón sin portal → **anotado** para verificar si replica el síntoma (no se tocó). Suite **250/250** (+1 test del `compact`) + lint limpio + coverage lib 96.4% / hooks 99.1% (meta ≥80%).
- **Quick wins 2 (2026-08-13)** — tres features del área análisis sobre la base de la sesión anterior:
  1. **Detalle por modo (filtro por signo)** — el memo `filtered` de `Dashboard.js` filtra `amount > 0` en modo **Ingresos** y `amount < 0` en **Egresos** antes del análisis → cards y tabla quedan homogéneos (antes la tabla mezclaba créditos y débitos en ambos modos).
  2. **Modal de detalle de resumen** — click en un resumen desde la pestaña Resúmenes (antes solo seteaba `selectedId` sin cambiar la vista) abre `SummaryDetailModal.js` (patrón de `QuoteModal`: overlay, cierre con X/Esc/backdrop) con el nombre del resumen, **toggle propio Ingresos↔Egresos** y adentro un `Dashboard` con `summaryId` fijo; `Dashboard`/`FiltersBar` ganan `hideSummaryFilter`/`hideSummary` para ocultar el dropdown "Resumen" dentro del modal (el resto de los filtros operan). Wiring: `ResumenesView` maneja el estado `detail` y pasa `onOpenDetail` a `UploadSummaries`/`SummaryItem`.
  3. **Toggle de pagos de tarjeta (persistido)** — `buildAnalysis`/`buildIncomeAnalysis` aceptan `{ includePayments }` (antes re-excluían la categoría `'Pagos'` internamente, con lo que el toggle no tendría efecto); `Dashboard.js` lee `localStorage['mandarina:include-payments']` al montar, `working`/`analysis` respetan el flag y el aviso cambia de "Se excluyen N pagos…" a "Incluyendo N pagos…" con botón Incluir/Excluir (solo egresos). **249/249 tests** (+11), lint 0 issues, coverage lib 96.4% / hooks 99.1% (meta ≥80%). **Nota**: el modal monta un segundo `Dashboard` (refetch de transacciones) — aceptado como quick win, anotado como optimización futura.
- **Quick wins de análisis (2026-08-12)** — Vista de **Ingresos** separada de Egresos: `buildIncomeAnalysis` en `lib/analysis.js` (créditos solos, mayor ingreso ARS/USD, desglose por categoría/origen, tendencia de créditos), pestañas **Ingresos/Egresos/Resúmenes** en la nueva `ResumenesView.js`, y `SummaryCards`/`SpendingCharts` con variante `ingresos`. Navegación simplificada a 2 items (Resúmenes, Inversiones), tour reescrito (paso "Egresos" + mención de las dos vistas). **Fix preexistente**: `SummaryCards.js` usaba `fmtUSD` que no existe → `fmt(n, 'USD')` (dejaba 13 tests de Dashboard en rojo). Copy de pagos excluidos aclarado en `Dashboard.js` ("Se excluye(n) N pagos de tarjeta de los totales (categoría 'Pagos')"). Toast de top 3 posiciones con **segmentos coloreados por variación** (verde/rojo/neutro, tipo `info` en vez de `success`).
- **Fase 8 de `improvements.md` — limpieza final y documentación CERRADA** (2026-08-12). **Limpieza**: borrados `backend/supabase/.temp/` (5.6MB, catálogos pgdelta), `frontend/coverage/` (salida gitignored) y `backend/supabase/snippets/` (dir vacío); `frontend/frontend/`/`frontend/build/` ya no existían. **`.opencode/` fuera del repo** (decisión del usuario): `opencode.json` destraqueado (`git rm --cached`, queda en disco) e ignorado en `.gitignore` raíz; la memoria (AGENTS.md + HANDOFF.md) sigue auto-cargándose localmente. **README raíz reescrito** con stack real (Vite/Vitest/Deno/Tailwind), estructura actual, comandos, env vars y orden de deploy; `frontend/README.md` sumó `coverage`/`lint`/`format`. **`.md` obsoletos borrados** (decisión del usuario): `fase7.md` (plan de fase cerrada), `compromised.md` (deps del supply-chain histórico; hoy cubre `npm audit` + Dependabot) y `AGENTS_TEAM.md` (blueprint de subagentes sin agentes activos); referencias ajustadas en `AGENTS.md`, `HANDOFF.md`, `frontend/eslint.config.js` y `improvements.md`. **Cierre**: `improvements.md` Fase 8 + checklist global `[x]`, `TODO.md` sin items muertos, `fase8.md` archivado en raíz.
- **Auth** — signup/login con email+password (`Auth.js`), detección de sesión en `App.js`.
- **Esquema DB** — `0001_init.sql`: `card_summaries`, `transactions`, `consumption_analyses` + RLS por dueño.
- **Storage + subida de archivos** — `0002_storage.sql`: bucket privado `card-resumes` + policies RLS por carpeta de usuario. Componente `UploadSummaries.js` (subida PDF/CSV/XLSX + listado + descarga).
- **Status de procesamiento** — `0003_parse_status.sql`: columnas `status` + `error` en `card_summaries`.
- **Edge Function `parse-summary`** — parseo CSV (con auto-detección de separador) y XLSX (SheetJS); normaliza fechas y montos.
- **Mapeo de columnas** — formato real del usuario (saldos + `RELEASE_DATE;TRANSACTION_TYPE;REFERENCE_ID;TRANSACTION_NET_AMOUNT;PARTIAL_BALANCE`), aliases y normalización de `_`→` `.
- **Análisis de consumo** — categorización por keywords (`transactions.category`), `buildAnalysis` → `result` JSONB (period, totals, maxExpense, maxCredit, byMerchant, byCategory, byDay, balanceTrend). `0004_analysis_unique.sql`: constraint única `consumption_analyses(summary_id)`.
- **Frontend análisis** — `AnalysisSummary.js` (créditos/débitos/neto, mayor gasto/ingreso). Verificación end-to-end con CSV real. _(superado por el dashboard, se eliminó)_
- **Dashboard visual** — UI estilo Notion con Chart.js (`chart.js` + `react-chartjs-2`): layout sidebar + panel principal, cards de métricas, línea `balanceTrend`, doughnut `byCategory`, barras `byMerchant`, tabla de transacciones. Restyling `index.css` + `App.css`.
- **Moneda por transacción** — `0005_currency.sql`: columna `currency` (ARS/USD) en `transactions`. `buildAnalysis` separa totales/agregados ARS y un bloque USD; dashboard muestra card "Gastos USD" + columna de moneda con formateo por moneda.
- **Parseo PDF (BBVA VISA + MASTERCARD)** — extracción posicional con `unpdf` (x,y), tablas de detalle `FECHA/DESCRIPCIÓN/PESOS/DÓLARES` por coordenadas, fechas `DD-MMM-YY`→ISO, montos negativos (gasto), categorías Impuestos/Pagos/Suscripciones. Verificado e2e: VISA ARS `-534.577,35` (7 movs), MASTERCARD ARS `-33.122,53` + USD `-17,61` (11 movs).
- **GitHub** — repo `alvarezem/fimplify` (privado) creado vía `gh` CLI; `.gitignore` raíz (ignora `examples/` con datos financieros reales); `README.md` del proyecto; commit `10334cc` y push de `master` a `origin`.
- **Vista global por períodos (multi-resumen) + filtros** — el dashboard computa todo client-side desde las transacciones del usuario (`lib/analysis.js`, port de `buildAnalysis`); **"Todos los resúmenes"** por defecto (drill-down a un resumen individual); columna "Resumen" (archivo) en la tabla global; refresco tras subir vía `onDataChanged`/`refreshKey`. El backend no cambió; `consumption_analyses` sigue en DB pero el dashboard ya no la lee.
- **Mejoras al dashboard** — pagos de tarjeta (categoría `Pagos`) **excluidos de todos los totales/charts/tabla** con aviso de cuántos se excluyeron (`EXCLUDED_CATEGORIES` en `lib/analysis.js`); chart de línea ahora **"Gastos acumulados"** (|débitos| acumulados, eje ≥0, `expenseTrend`) en vez de balance; **filtros como dropdowns** custom compactos (`Dropdown.js` + `FiltersBar.js`: Período con rango custom, Categorías multi-select con checkboxes, Moneda, búsqueda de comercio).
- **Categorización ampliada + backfill** — reglas movidas a `backend/supabase/functions/_shared/categorize.ts` (fuente única, comentario de sync con SQL); nuevas categorías **Seguros, Combustible, Telecom, Supermercados, Entretenimiento, Gastronomía, Transporte, Farmacias, Educación, Gimnasio, Salud** (keywords afinadas con los merchants reales de la DB); migration `0006_reclassify.sql` actualizó las txs existentes (CASE idéntico a TS); edge function `parse-summary` redeployada; verificado con 25 merchants reales sin regresiones.
- **Quitar "Créditos"** — eliminadas las cards **Créditos**, **Neto** y **Mayor ingreso** (los créditos de tarjeta no aportan) y la lógica muerta en `lib/analysis.js` (`maxCredit`, `avgPerDay`, `balanceTrend`). Nuevas cards de mayor gasto por moneda: **"Mayor gasto ARS"** + **"Mayor gasto USD"** (con `usd.maxExpense`, comercio en `sub`).
- **Orden del detalle + clic en gráficos** — tabla con **headers clicables** ▲/▼: Fecha y Monto de mayor/menor, Descripción alfabética, Categoría alfabética y agrupada (`SortableTh` + estado `sort` en `Dashboard.js`, aplicado solo a las filas). **Clic en los gráficos** filtra el detalle y hace scroll a la tabla: doughnut→categoría (toggle si ya está sola), barras→comercio (vía buscador), línea→ese día (período custom). Captions de ayuda bajo cada chart.
- **UX de filtros** — dropdown **Resumen** en la barra de filtros sincronizado con la sidebar (una sola fuente de verdad, `selectedId`); botón **"Limpiar filtros"** (visible solo con filtros activos) que resetea período/fechas/categorías/moneda/búsqueda y vuelve a "Todos los resúmenes"; **logo + nombre como link** a la página principal (reset en la misma página: limpia filtros, deselecciona resumen, cierra drawer y sube al top; `resetKey` + scroll en `App.js`). La columna "Resumen" de la tabla global muestra el archivo y ahora **Moneda y Resumen son sortables** (junto con Fecha, Descripción, Categoría y Monto); títulos de columnas en **Title Case** ("Fecha, Descripción, Resumen, Categoría, Moneda, Monto"). El filtro de moneda sigue en el dropdown global (ya filtra cards, charts y el detalle).
- **Deploy Vercel por CLI** — el proyecto **no tiene integración Git** en Vercel (los deploys son solo por `vercel deploy --prod` desde `frontend/`); el deploy no se actualiza solo con `git push`. Diagnóstico por bundle desplegado (grep de strings nuevos en `main.*.js`). Build verificado con `CI=true npm run build` (tailwind CLI + react-scripts).
- **Pulido visual (TailwindCSS)** — 1er pase: Auth + Dashboard con cards/charts/theme. 2do pase: layout + sidebar + `UploadSummaries` + responsive — header sticky con blur, drawer móvil (`<lg`), drop zone de subida auto-subida, pills de status, `App.css` eliminado (todo Tailwind). **Tema claro/oscuro**: toggle sol/luna (`ThemeToggle.js`) en header y pantalla Auth; dark mode por clase (`@custom-variant dark`), script anti-flash en `public/index.html`, persiste en `localStorage` y arranca siguiendo al sistema; Chart.js theme-aware vía prop. `index.css` es el entry de Tailwind vía `@tailwindcss/cli` → `src/index.generated.css`.
- **Toggle del panel lateral** — botón en `lg+` (junto al menú hamburguesa) para esconder/mostrar el `aside` de resúmenes; estado persistido en `localStorage` (`fimplify-sidebar`); convive con el drawer móvil actual (`App.js`). _(reemplazado por la navegación en 3 secciones: resúmenes como vista, sin aside ni drawer)_
- **Tests (Jest + React Testing Library)** — 29 tests en 5 suites: `lib/analysis.js` (totales, agregados, exclusión de Pagos, bloque USD, tendencia), filtros del dashboard (moneda, categoría, búsqueda, resumen, limpiar filtros, orden por headers), `UploadSummaries` (estado vacío, listado, subida+parseo), `Auth` (login/signup/error) y responsive. `App.test.js` ya no es boilerplate.
- **Test de responsive** — mock de `matchMedia` (setup global) + `supabase`: drawer móvil con hamburguesa `<lg`, sidebar colapsable en `lg+` persistido en localStorage, y que la tabla no desborde (`overflow-x-auto`) con cards en grilla responsive.
- **Seguridad: revisión de `compromised.md`** — se verificó que **ninguna versión comprometida** está instalada (keyv@4.5.4, flat-cache@3.2.0, file-entry-cache@6.0.1 son transitivos de `eslint`, todas anteriores a las infectadas; el resto de paquetes listados no existen en el repo; `backend` limpio). **Regla**: `overrides` en `frontend/package.json` fijan `file-entry-cache`/`flat-cache`/`keyv` a esas versiones seguras para que `npm` no los actualice.
- **Cleanup backend** — `@supabase/server` verificado sin uso en fuentes y removido de `backend/package.json` (y del lockfile).
- **Animaciones full + fixes + toasts + micro-animaciones** — count-up de montos con easing y stagger completo (opción ganadora vs. subtle); **fix de ingresos**: créditos (montos ≥ 0) excluidos de `byCategory`/`byMerchant`/`usd.byCategory` en `lib/analysis.js` (solo gastos en charts y agregados); fix cursor pointer en buttons (`index.css`); **sistema de toasts** (`Toast.js`: `ToastProvider`/`useToast`, stack top-right, tipos success/error/info, auto-dismiss, `aria-live`) en subida de resúmenes, errores y "Sesión cerrada"; **micro-animaciones**: boot splash con logo, spinner en submit de Auth y en subida, skeletons shimmer en lista de resúmenes, micro-stagger al cambiar filtros en Dashboard, `active:scale` en botones. Deploy único a producción por `master`.
- **Navegación en 3 secciones** — Resúmenes deja de ser panel lateral fijo/drawer y pasa a ser una **vista centrada** (`mx-auto max-w-2xl`) accesible desde el rail y la bottom nav; al elegir un resumen navega a Costos con ese filtro (el dropdown "Resumen" del Dashboard sigue funcionando). **Rail expandible por defecto** en el primer uso (localStorage persiste la preferencia) con toggle inferior con borde + label "Colapsar"/"Expandir". **Bottom nav móvil**: grilla de 3 (Costos, Inversiones, Resúmenes) con el **logo fimplify central elevado** que vuelve al inicio.
- **Tema claro más suave** — fondo gris (`--color-surface: #f1f5f9`, gradientes sin blanco puro: `from-slate-100 via-slate-50`), cards blancas con sombras suaves (`shadow-sm`/`shadow-md` sin glow teal), toasts/dropdowns/inputs en `slate-50`. Dark mode intacto.
- **Tests ampliados** — 36 tests en 5 suites: navegación del rail (3 secciones, expandir/colapsar con default expandido), bottom nav con logo central, vista de Resúmenes centrada (sin drawer), créditos excluidos de agregados, toasts de subida.
- **Renombrar resúmenes** — edición inline con lápiz por fila en la lista de Resúmenes: el nombre se vuelve un input (Enter/✓ guarda, Esc/X cancela, `blur` no; touch-friendly); valida nombre no vacío y máx 100 chars; update de `card_summaries.file_name` (RLS `update_own` ya existía, sin migración), toast de éxito/error, se propaga al dropdown "Resumen" y a la columna de la tabla del Dashboard (leen `file_name` vía join). El archivo en storage (`file_path`) no se toca; el parseo no se re-ejecuta.
- **Rebrand Mandarine + navbar estilo YouTube** — la app pasa de **Fimplify** a **Mandarine** (`mandarine.app`, nickname "Mandi" para el futuro asistente IA). **Logo**: ícono de mandarina SVG (cuerpo naranja + hojas) reutilizado como componente `Logo` (también en Auth, BootSplash y bottom nav) y `favicon.svg`; `logo192/512.png` regenerados desde el SVG (`rsvg-convert`). **Paleta por modo**: escala `brand` en `index.css` — claro = naranja mandarina (`#f97316`), oscuro = verde/teal actual (`#0d9488`); acentos `teal-*` → `brand-*` en 9 archivos y colores hardcodeados de Chart.js (línea/doughnut/barras) ahora sensibles a `dark`. **Navbar estilo YouTube (desktop)**: el header lleva [≡] (hamburguesa) + logo+wordmark "Mandarine" a la izquierda (clic = inicio); el hamburguesa alterna el rail expandido ↔ solo-íconos (estado persistido, el chevron inferior del rail se eliminó) y el título de vista queda solo en móvil. **Claves migradas** a `mandarine-theme` / `mandarine-rail-expanded` con fallback de lectura a las viejas `fimplify-*` (incluido el script anti-flash de `index.html`); `manifest.json` con `theme_color #f97316` y `background_color #fff7ed`. Título/descripción/metas → Mandarine. La parte móvil (drawer de navegación) se difirió a `TODO.md`.
- **Tema oscuro unificado en naranja + logo gajo** — feedback del usuario: el dark mode (teal) "desentonaba" con el logo naranja. **Unificación**: la escala `brand` y `--color-accent/accent-soft` quedan **naranja en ambos modos** (se eliminaron los overrides teal del `.dark` en `index.css`); los colores de Chart.js (línea/doughnut/barras) dejaron de depender de `dark` → siempre naranja `#f97316` (hover `#ea580c`, `rgba(249,115,22,…)`). **Logo → gajo de mandarina**: el ícono pasó de fruta entera a un **gajo en forma de crescent** (cáscara `#f97316` + pulpa `#fdba74` + fibras), aplicado en `Logo` (header/bottom nav/BootSplash/Auth), `ComingSoon` y `favicon.svg`; `logo192/512.png` regenerados. Infra del rebrand (dominio `mandarine.app`, rename GitHub/Vercel) documentada en `TODO.md` para ejecutar en otra sesión.
- **Rebrand de grafía "Mandarine" → "Mandarina"** — grafía corregida a **"Mandarina"** en todos los textos visibles: `Auth.js` (h1), `App.js` (wordmark del header + tooltip), `index.html` (title/description), `manifest.json`, `README.md`, `App.test.js`. Claves de localStorage migradas a `mandarina-theme` / `mandarina-rail-expanded` con fallback de lectura a `mandarine-*` y `fimplify-*` (incluido el script anti-flash de `index.html`). El dominio planeado para la infra pasa a `mandarina.app`.
- **Logo: nuevo gajo de mandarina (flat)** — rediseñado el gajo (el crescent anterior no convencía): **silueta con punta** y **cáscara** en la espalda (arco naranja `#f97316` con **poros** `#ea580c`), **pulpa** `#fdba74` con **2 fibras** hacia la punta. Aplicado en `Logo` (`Sidebar.js`) y `favicon.svg`; `logo192/512.png` regenerados (`rsvg-convert`). _(quedó pendiente la revisión visual del usuario)_
- **Login mejorado + animaciones + signup no básico** — `Auth.js` reescrito: animación de entrada (`animate-fade-in-up`) en logo, título y card, y **re-trigger al alternar login↔signup** (remount con `key`); campo **confirmar contraseña** (solo signup); **validación client-side** (email válido, contraseña ≥8, match) con errores inline por campo (`aria-invalid` + `role="alert"`); indicador de **fuerza de contraseña** (barra 4 segmentos + etiqueta Débil/Media/Buena/Fuerte); toggle **mostrar/ocultar contraseña**; pantalla de éxito post-signup **"Casi listo"** ("Revisá tu email") cuando Supabase devuelve `user` sin `session` (confirmación por email). Tests ampliados (42 en 5 suites).
- **Login con Google (OAuth)** — botón **"Continuar con Google"** en `Auth.js` → `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.origin } })`; la sesión la captura `onAuthStateChange` de `App.js` sin cambios. Mock + test del flujo en `Auth.test.js`. _(La activación del provider, las credenciales y las URLs de callback se completaron después — ver entrada de Google OAuth configurado y verificado abajo.)_
- **Deploy Vercel — quirk de `rootDirectory`** — el proyecto tiene `rootDirectory: "frontend"`. Deployar desde `frontend/` duplica el path (`frontend/frontend`) y el build cae con `react-scripts: command not found` (npm install no corre porque no encuentra `package.json`). El flujo correcto es `vercel deploy --prod` **desde la raíz del repo** (`rootDirectory` se aplica sobre el repo). Verificado: `fimplify.vercel.app` sirve `main.b96763d5.js` con `Mandarina` (3) y 0 `Mandarine`.
- **Hamburguesa del header alineada con el rail** — el [≡] ahora vive en un slot fijo de `w-16` con `flex justify-center` en el grupo izquierdo del header (con `-ml-4 sm:-ml-6` para cancelar el padding del header y arrancar en x=0). Su centro (32px) coincide con el centro de la columna de iconos del rail **plegado** (`w-16`) y se mantiene **consistente** al expandir; el logo + wordmark fluyen después del slot.
- **Frase pegajosa de marca** — branding principal: **"A tu plata, sacale todo el jugo"** en el subtítulo del Auth (`Auth.js`) y en la meta description de `index.html`. Sección Resúmenes: **"Tus números, sin cáscara"** como subtítulo de la vista (heading en `summariesView`, `App.js`). Wordmark del header queda solo "Mandarina".
- **Mensaje de despedida al logout** — el toast de `handleSignOut` (`App.js`) pasó de "Sesión cerrada" a **"¡Nos vemos!"**.
- **Plan de inversión (meta % vs actual %)** — módulo de la sección **Inversiones** (reemplaza el `ComingSoon` de `App.js`) que replica el Excel personal del usuario. **Backend**: migration `0007_investment_plan.sql` (tabla `portfolio_plan` con RLS por dueño + unique `(user_id, symbol)`); Edge Functions **`quotes`** (proxy de cotizaciones: acciones/CEDEARs/bonos → Yahoo Finance `query1/2.finance.yahoo.com/v8/finance/chart/{SYM.BA}` sin API key, con fallback de host y retry ante 429; dólares CCL/MEP → dolarapi.com, ambas sin keys) e **`import-plan`** (parsea XLSX por aliases de header Ticker | % Meta | Tenencia, ignora metadatos previos y **reemplaza** el plan del usuario; nunca importa precio — siempre live). **Frontend**: `lib/plan.js` (`buildPlan`/`distribute`/`buyAmount` con semántica `x = (T·V − v)/(1 − T)` por aporte de cash nuevo sin vender, señala sobre-asignación) + `InvestmentPlan.js` (CRUD inline por fila, importar XLSX, **precios siempre live** con override manual solo para activos sin cotización —FCI/efectivo—, toggle de visualización **ARS ↔ USD** usando CCL o MEP con cotización live, y **"Tengo para comprar"**: presupuesto manual que ordena los faltantes desc y permite marcar compras que actualizan cantidades y recalculan en vivo). 17 tests nuevos (7 suites, 59 en total). Desplegado: migration + edges en Supabase (proyecto hosted) y bundle en Vercel (`main.47be1705.js`).
- **Cotizaciones en vivo (estilo Bull Market) + migración a BYMA** — la sección **Inversiones** pasa a tener **2 sub-menús (tabs)** vía `InvestmentsView.js`: **Plan de inversión** (sin cambios) y **Cotizaciones en vivo** (`MarketQuotes.js`): resumen estilo Bull Market con **patrimonio total** (ARS↔USD con CCL/MEP), **Δ diaria ponderada** (`portfolioChangePct` en `lib/plan.js`: pondera el cambio por el valor de ayer), cards **MEP/CCL** (dolarapi), **doughnut de asignación** (Chart.js, colores de la paleta mandarina) y **tabla** de tenencias con precio, variación ▲▼, volumen implícito, cantidad, valor y % de cartera con mini-barra; estados vacío/sin-precio y botón de refresco. **Backend**: la edge `quotes` **reemplaza Yahoo Finance (bloqueado, 429 global) por BYMA Open Data** — `POST open.bymadata.com.ar/.../bnown/fichatecnica/especies/cotizacion` con `{symbol, settlementType:"2"}` → `{trade, previousClosingPrice, tradeVolume, denominationCcy}` para cualquier activo (CEDEARs, acciones, bonos; símbolo inexistente → `data: []`), con caché in-memory 60s, `Promise.allSettled` y headers tipo pyOBD; MEP/CCL siguen vía dolarapi. La edge **`import-plan`** se endureció: lee el plan previo antes de borrar y hace **rollback** si el insert falla, exponiendo el error real. Verificado end-to-end con usuario de test (servicio): `import-plan` importa los 12 activos del `Finance.xlsx` real y `quotes` devuelve precios BYMA de los 12 + AL30 + MEP/CCL; usuario de test eliminado después. 9 tests nuevos (9 suites, 68 en total). Desplegado: edges en Supabase (proyecto hosted) y bundle en Vercel (`main.8799c49c.js`, `fimplify.vercel.app`).
- **Iteración de pulido: MEP/CCL compacto, estado compartido, ordenamiento y leyenda** — feedback del usuario sobre Inversiones. **MEP/CCL**: se eliminan las 2 cards grandes de Cotizaciones y se reemplazan por un **dato compacto** de referencia ("MEP 1.521,1 · CCL 1.575,8", solo venta — dolarapi trae compra/venta casi iguales, ~1 ARS, porque es el feed de referencia) que aparece **en ambas vistas** al costado del toggle ARS/USD; la **elección del dólar (CCL/MEP) vive solo en el Plan de inversión** y se **comparte** con Cotizaciones (estado `display`/`rateMode` levantado a `InvestmentsView` y pasado por props; `MarketQuotes` ya no muestra selector de dólar). **Ordenamiento**: ambas tablas ahora ordenan por cualquier columna (headers clicables ▲/▼) con el componente compartido `SortableTh.js` (patrón del Dashboard): Plan → Activo/Precio/Cantidad/Valor/Actual/Meta/Gap/A comprar; Cotizaciones → Activo/Precio/Variación/Cantidad/Valor/% cartera; default = orden del plan, numérico con sin-precio al final. **Leyenda del doughnut**: sin `max-h` ni scroll, muestra el listado completo. Bonus: las cotizaciones se buscan aunque el plan esté vacío (para mostrar MEP/CCL desde el primer uso). 5 tests nuevos (9 suites, 73 en total). Desplegado en Vercel (`main.a409ba5d.js`, `fimplify.vercel.app`).
- **Estrategias de prioridad de compra + "Var. diaria"** — feedback del usuario: el modo "Tengo para comprar" siempre recomendaba **VIST** (diagnóstico: la estrategia era "mayor faltante en $" → `buy = (T·V − v)/(1 − T)`, y VIST era el mayor faltante en pesos del plan real; SPY/GLD/MSFT estaban sobre la meta con `buy=0`). **Fix + feature**: `distribute(budget, items, strategy)` en `lib/plan.js` recibe una estrategia configurable con 6 opciones filtradas a los que tienen faltante (`buy > 0`) y ordenadas con sorters propios — **faltante** (mayor faltante $, default), **gap** (mayor faltante %), **billetera** (mayor % de cartera), **peso** (mayor peso objetivo), **barato** (menor precio por unidad) y **caro** (mayor precio; sin precio va al final en las 2). `InvestmentPlan.js` agrega el selector **"Priorizar por"** junto al presupuesto y actualiza el hint ("…según tu prioridad, se recalcula en vivo al comprar"); el orden de pasos y la cobertura se recalculan en vivo. **Claridad**: el header de la tabla de Cotizaciones pasó de "Variación" a **"Var. diaria"** (es el Δ de hoy). 8 tests nuevos (9 suites, 81 en total). Desplegado en Vercel (`main.3ddc2e01.js`, `fimplify.vercel.app`, verificado por grep del bundle).
- **Rebrand de infraestructura gratis (sin comprar dominio)** — el usuario descartó comprar `mandarina.app` (siempre se paga, ~US$12-15/año; prefiere 100% gratis pero prolijo). Se renombró todo lo externo sin gasto: proyecto Vercel `fimplify` → **`mandarina`** y repo GitHub → **`alvarezem/mandarina`** (remote local actualizado). **URL principal: `mandarina-fi.vercel.app`** (el nombre ideal `mandarina.vercel.app` ya estaba tomado por otro equipo en Vercel; `mandarina-fin`/`mandarina-finanzas` se probaron libres pero se descartaron, y el dominio legacy `fimplify.vercel.app` se **eliminó** — el usuario no tiene nada conectado ahí). Verificado: `mandarina-fi.vercel.app` sirve `main.3ddc2e01.js` y las viejas URLs dan 404. `TODO.md` actualizado: queda como único paso futuro (opcional) comprar el dominio custom.
- **Toast de logout en verde + gráficos de precio por activo** — dos features. **(1) Toast de logout**: "¡Nos vemos!" (`App.js`) pasó de `type: 'info'` (naranja brand, se leía como warning/error) a **`type: 'success'`** (verde esmeralda, consistente con dark mode); test que verifica las clases `emerald` del toast. **(2) Gráficos de precio**: la edge **`quotes`** acepta ahora `body.history = { symbol, range }` y trae el histórico OHLCV de **BYMA** (`chart/historical-series/history`, Unix **seconds** — ojo: millis da `no_data` —, formato TradingView `{s,t,o,h,l,c,v}`) con mapeo **1S→D 7d · 1M→D 35d · 3M→D 95d · 1A→W 370d**, normalizado a `{ points: [{t,o,h,l,c,v}] }` (t en ms), cache 5 min y `no_data`→`[]`; el rango **1D/intradía no existe en BYMA free** (res 1/5/60 → `no_data`), anotado. Frontend: `lib/history.js` (puras `normalizeHistory`/`formatPointDate`/`RANGES`), componente reutilizable **`PriceChart.js`** (línea/área naranja con Chart.js `Line`, selector de rangos, estados loading/error/sin-datos, prop `compact`), y `MarketQuotes.js` con **dos formas de ver el gráfico**: click en el **símbolo** expande el panel **inline** debajo de la fila (uno a la vez) y el botón **⤢** abre el mismo gráfico en un **modal in-app** (backdrop, cierra con X/Esc); ambos comparten una única fetch. 10 tests nuevos (10 suites, 91 en total). Verificado e2e con usuario de test (VIST 3M=65 pts diarios, QQQ 1A=54 pts semanales, spot quotes intactos; usuario borrado). Desplegado: edge `quotes` en Supabase (proyecto hosted) y bundle en Vercel (`main.ea0e1a46.js`, `mandarina-fi.vercel.app`, verificado por grep).
- **Sesión del día en el gráfico + gráfico en la moneda del toggle + orden compartido y persistente + toasts de saludo y top 3** — feedback del usuario sobre Inversiones. **(1) Franja de sesión**: la edge `quotes` expone ahora `open/high/low/prevClose/closingPrice/tradeHour` (campos BYMA ya presentes) y `PriceChart.js` muestra arriba del gráfico (inline y modal) una franja compacta con **Precio** + **Var. diaria ▲▼** destacados y abajo **Apr · Cierre prev · Máx · Mín · hoy HH:MM**; sin cotización del día, solo el gráfico. **(2) Moneda del gráfico**: con `display = USD` y tasa disponible, los **puntos del histórico** (o/h/l/c) y la **franja** se escalan ÷ rate por `useMemo` (sin refetch al alternar ARS↔USD), mismo criterio que la tabla; el tooltip del chart también formatea en la moneda activa. **(3) Orden compartido y persistente**: nuevo `lib/planSort.js` con `DEFAULT_PLAN_SORT = { key:'target_weight', dir:'desc' }` (**% Meta mayor→menor**), `loadPlanSort/savePlanSort` en localStorage clave `mandarina:plan:sort:<userId>` (sobrevive logout/login y se separa por usuario); `InvestmentsView` levanta `sort`/`onSort` y las pasa a **ambas** vistas → el orden elegido en el Plan (headers clicables) **se refleja en Cotizaciones en vivo**; cada vista valida la clave (si no le corresponde, fallback a % Meta desc). La **Asignación** (doughnut + leyenda) ahora usa `sortedItems` para coincidir con la tabla. Captions: Plan "Orden: % Meta mayor→menor · cambiá con los encabezados de la tabla (se recuerda)" y Asignación "Mismo orden que tu plan · se cambia desde Plan de inversión". **(4) Toasts al login**: el toast de logout ahora es una **mano saludando** (ícono SVG `wave`); al **iniciar sesión** (`SIGNED_IN`) se muestra "¡Bienvenido/a a Mandarina!" (primera vez, por `created_at`≈`last_sign_in_at`) o "¡Volviste!", y enseguida un segundo toast con la **variación diaria de las top 3 posiciones** por % Meta ("Tus posiciones: VIST ▲1.20% · QQQ ▼0.50% · KO —", ícono `trend`); sin plan o sin precios se salta en silencio; el **refresh con sesión guardada no tostadea** (solo evento `SIGNED_IN`). 12 tests nuevos (10 suites, 103 en total). Verificado e2e con usuario de test (spot VIST con open/high/low/prevClose/changePct/tradeHour + histórico 3M=65 pts; usuario borrado). Desplegado: edge `quotes` en Supabase (proyecto hosted) y bundle en Vercel (`main.a786be00.js`, `mandarina-fi.vercel.app`, verificado por grep).
- **Orden de columnas del Plan + emoji "¡Volviste!" + nota de cobertura + hover naranja del theme + borrar resúmenes** — feedback del usuario sobre Inversiones y Resúmenes. **(1) Columnas del Plan**: reordenadas a **Activo · Precio · Meta · Actual · Gap · Cantidad · Valor · A comprar** (la Cantidad pasó de 2da a 6ta posición, quedando junto a Valor, y el valor en plata de "A comprar" se reemplazó por **solo las unidades** `≈X u` en brand, con "—" cuando no hay faltante). **(2) Saludo**: el toast de vuelta pasó a **"¡Volviste! 😂"** (Bienvenido intacto). **(3) Nota de cobertura**: "· X de Y activos con precio" (junto al Δ diario de Cotizaciones) se muestra **solo cuando hay activos sin precio** (`pricedCount < builtItems.length`); con 12/12 desaparece. **(4) Theme toggle**: el botón sol/luna (`ThemeToggle.js`, login + header) pasó de hover slate a **hover naranja brand** (`hover:border-brand-300 hover:bg-brand-50 hover:text-brand-600` + variantes dark). **(5) Borrar resúmenes**: migration `0008_storage_delete.sql` agrega la policy **DELETE** de `storage.objects` para la propia carpeta del usuario (el bucket `card-resumes` solo tenía select/insert; el delete de `card_summaries` con cascade ya existía en `0001_init.sql`); `UploadSummaries.js` suma un botón 🗑 por fila con **confirmación inline** ("¿Borrar? Sí/Cancelar", patrón del renombrar): borra el archivo del storage (tolerando no-encontrado) + la fila (cascade a transactions/consumption_analyses), refresca la lista, propaga `onDataChanged`, resetea la selección si era la activa y tostadea éxito/error. 9 tests nuevos (12 suites, 112 en total). Desplegado: migration en Supabase (proyecto hosted) y bundle en Vercel (`main.a461a342.js`, `mandarina-fi.vercel.app`, verificado por grep: "¡Volviste!" con 😂 `\ud83d\ude02`, "activos con precio", "¿Borrar?" `\xbf`, "Eliminar", `≈` `\u2248`, "hover:border-brand-300").
- **Quickwins: sin manito en "¡Volviste!" + difuminado del tema solo en hover** — **(1) Manito**: el toast de vuelta (`App.js`) pasó de `icon: "wave"` a `icon: "none"` solo en el caso "¡Volviste!" (el "¡Bienvenido/a!" y el logout "¡Nos vemos!" conservan la mano); `Toast.js` ahora ignora el ícono cuando `icon === "none"`; el "😂" queda. **(2) Difuminado naranja**: el blob decorativo `bg-brand-500/20 blur-3xl` de la esquina del Auth (que envolvía siempre al ThemeToggle) se reemplazó por un **glow del propio botón** dentro de un wrapper `group`: `opacity-0 group-hover:opacity-100` con transición, solo visible al pasar el mouse; el blob emerald de abajo-izquierda queda como ambiente. 1 test nuevo (12 suites, 113 en total). Desplegado en Vercel (`main.7cbf83e2.js`, `mandarina-fi.vercel.app`, verificado por grep: `icon:"first"===i?"wave":"none"`, `group-hover:opacity-100`).
- **Nuevo logo PNG con fondo removido** — el logo pasó de ser el SVG del gajo (`Logo` en `Sidebar.js`) a la imagen `examples/logo.png` (mandarina naranja, 385×416) **sin fondo**: se removió el fondo crema uniforme `#FEE8CC` por **floodfill desde los bordes** (fuzz 8%) + máscara con blur 0.6px (bordes anti-aliased suaves), `CopyOpacity` y `-trim`. Verificado por stats (no visual): **342×357**, esquinas/bordes transparentes, silueta sólida **sin agujeros interiores** (0 píxeles negros tras floodfill), contenido naranja preservado (74.343 px opacos), borde suave (2.764 px semi-transparentes, sin halo). Derivados regenerados desde el nuevo transparente: `logo192.png`, `logo512.png` (centrados, sin distorsión) y `favicon.ico` (16/24/32/48/64). El componente `Logo` ahora renderiza `<img src="/logo.png" object-contain aria-hidden>` (aplica a header, Auth, splash y bottom nav en un solo punto); `index.html` apunta el icono a `favicon.ico`, `manifest.json` usa los PNG, y se eliminó `favicon.svg`. `examples/` sigue en `.gitignore` (solo se versionan los assets de `public/`). Desplegado en Vercel (`main.7c64cc43.js`, `mandarina-fi.vercel.app`; `/logo.png` servido con alpha min=0 mean=0.609, `/favicon.ico` y `/logo192.png` 200).
- **Google OAuth configurado y verificado + linkeo de cuentas OK** — se completó la parte manual del OAuth de Google: provider activado en Supabase (`external.google: true`, verificado vía `GET /auth/v1/settings`) y credenciales OAuth creadas en Google Console. Deploy en `mandarina-fi.vercel.app` (bundle `main.7c64cc43.js`, contiene `signInWithOAuth`/`Continuar con Google`). **Verificado 2026-08-07**: `GET /auth/v1/authorize?provider=google&redirect_to=…` responde **302 → accounts.google.com** con `client_id` y `redirect_uri` correctos, tanto para `https://mandarina-fi.vercel.app` como para `http://localhost:3000` (dev local apunta al Supabase hosted — Opción A); la pantalla de consentimiento de Google carga sin `redirect_uri_mismatch`. **Linkeo de cuentas confirmado por el usuario**: el login con Google se linkea a la cuenta existente del mismo email (1 solo usuario, sin duplicados) — comportamiento esperado por el **auto-linkeo por email verificado** de Supabase. Dev local con `supabase start` (Opción B) queda en hold en `TODO.md`. Sin datos personales en el repo: solo quedan refs públicos de infraestructura.
- **Edición de categoría con búsqueda + overrides por comercio + categorías custom** — el categorizado automático por keywords a veces falla; ahora el usuario puede corregir desde la fila del detalle de Costos. **(1) Dropdown de fila reescrito** (`Dropdown.js`): portal a `body` (no se recorta en la tabla), **búsqueda** con input (`searchable`), render-prop `children({ close, query })` para ítems que no deben filtrarse (`data-pinned`), `maxHeight` dinámico según espacio disponible, `openUp` si no entra abajo, y **fix del wheel**: ignora el scroll cuyo target está dentro del menú (scrollbar custom `.dropdown-scroll` en `index.css`). **(2) "Recordar para este comercio"**: tabla `merchant_overrides(user_id, merchant, category)` (migration `0010_merchant_overrides.sql`, unique `(user_id, merchant)`, RLS own) — al tildar el checkbox y elegir categoría, se guarda el override y se **reclasifican todas las txs de ese comercio**; `parse-summary` ahora lee los overrides del usuario y los aplica al parsear. **(3) "+ Nueva categoría…"**: tabla `custom_categories(user_id, name)` (unique `(user_id, name)`, RLS own) con input inline en el menú; las categorías custom y las de los overrides se suman a las opciones del dropdown. **(4) Regla Inversiones ampliada**: `categorize.ts` + `0006_reclassify.sql` → `merchant ~* 'rendimiento|bull market|broker'` (migration `0010` reclasificó las existentes), así "Transferencia enviada Bull Market Brokers Sa" vuelve a Inversiones. `App.js` pasa `session` a `Dashboard` (userId para overrides). 13 tests en `Dashboard.test.js` (guardar override recordado, crear categoría custom, búsqueda en el dropdown) + mock de las tablas nuevas en `App.test.js`. Deploy: push `11136dc` → Vercel, `supabase db push` (0010), `supabase functions deploy parse-summary`.
- **Emails con marca Mandarina (SMTP custom + templates)** — correos de auth 100% con marca. **SMTP propio configurado con Brevo**: host `smtp-relay.brevo.com`:587, user `b4ce04001@smtp-brevo.com`, SMTP key de Brevo, sender `contacto.mandarina.fi@gmail.com`, sender name **"Mandi from Mandarina-Fi"** (mantenido a pedido del usuario). Sin esto, el cambio de Supabase del 3-jun-2026 bloquea editar templates de email en free tier. **13 templates HTML** con marca (confirmación/reset/magic-link/cambio de email/invite + reauthentication + 7 notificaciones de seguridad) con subjects "Mandarina Fi", título "Bienvenido a Mandarina Fi", naranja `#f97316`, CTA inline y footer "A tu plata, sacale todo el jugo", versionados en `backend/supabase/templates/` y aplicados vía Management API (`PATCH config/auth` con `mailer_subjects_*`/`mailer_templates_*_content`). **Notificaciones de seguridad habilitadas** (password_changed, email_changed, identity_linked/unlinked; phone/MFA quedan off). **Logo**: Gmail no renderiza imágenes data-URI, así que el logo real del sitio (`frontend/public/logo.png`, el gajo) se hosteó en **Supabase Storage bucket público `branding`** (`mandarina-fi-logo-v1.png`, cache inmutable 1 año) y se embebe su URL en los templates. **Persistencia verificada** (GET +0s y +60s): subjects 13/13 y contenidos con logo 13/13 (sin tocar SMTP, que des-sincroniza los templates). **E2E OK**: signup con test user → email real llega al inbox, link de confirmación apunta a `mandarina-fi.vercel.app`, recover 200, `confirmation_sent_at` seteado.
- **Sistema de email arreglado** — el envío no funcionaba por `smtp_user` y `smtp_pass` mal configurados (era el gmail de login en vez del login de Brevo `b4ce04001@smtp-brevo.com`; y no estaba la SMTP key). Corregido en `config/auth` vía Management API y verificado e2e. **Lección**: los PATCH a `config/auth` que tocan SMTP re-sincronizan GoTrue y revierten subjects/contenidos de los templates a los defaults — por eso SMTP se tocó primero y los templates al final, y la verificación se hizo tras cada PATCH.
- **Infra de gestión y tokens** — token del **Management API** (`cli_alavarezem`) guardado en el **GNOME keyring** (no en `.env`), se recupera con `secret-tool lookup service "Supabase CLI"`; script `/tmp/opencode/apply-email-templates.mjs` lo lee del keyring (fallback a `backend/.env`) y aplica subjects+contenidos+flags. **Service role rotada**: la de `backend/.env` estaba obsoleta (JWT inválida), se refrescó con la `service_role` fresca del Management API y se actualizó en `backend/.env` (gitignored).
- **Autodeploy de Vercel desde GitHub** — el proyecto Vercel `mandarina` quedó conectado al repo `alvarezem/mandarina` (GitHub App, rama de producción `master`, solo producción sin previews de PRs). Antes el deploy era manual por CLI (`vercel --prod` desde `frontend/`). Ahora cada push a `master` despliega automáticamente a `mandarina-fi.vercel.app`. El CLI sigue funcionando como fallback. En el camino: `frontend/.vercel/project.json` apuntaba a un proyecto huérfano `frontend` (creado por un `vercel link` accidental), se restauró al proyecto `mandarina` y el proyecto basura se eliminó.
- **Metadata de resúmenes (tipo + período)** — `card_summaries` ahora clasifica cada resumen por **tipo** (`summary_type`, ex-columna `bank` renombrada en `0011_summary_metadata.sql`) y **período** (`period_month`/`period_year`). `parse-summary` autodetecta: tipo por aliases del nombre del archivo (VISA / MASTERCARD / AMEX / Billetera virtual —MercadoPago, uala, brubank, etc.— / Broker —iol, balanz, cocos…— / Banco), con fallback PDF sin alias → `Banco` y CSV/XLSX sin alias → sin clasificar; el tipo de tarjeta tiene prioridad sobre el banco (`bbva-visa.pdf` → VISA); y período = mes/año **modal** de las fechas de las transacciones. UI: badge `Tipo · mes año` en cada fila de Resúmenes con **editor inline** (select tipo/mes/año, persistido por update) y badge también en la columna Resumen de Costos. **Verificado**: suite de tests **Deno** nueva (`detection_test.ts`, 13 casos: aliases, mayúsculas/acentos, fallbacks, período modal/empate/vacío — destapó y fixeó un bug de fechas nulas → NaN en `detectPeriod`) + **matriz e2e de 21 casos** contra la función desplegada (aliases, PDF fallback, fixture de 2 meses → modal) + tests de frontend (122/122). La lógica de detección se extrajo a `detection.ts` (exportada y tipada, corre con Deno 2.9 vía `mise`).
- **Detección de marca por contenido del PDF** — además del alias del nombre, `parse-summary` ahora escanea el **texto de la página 1** del PDF (donde vive el encabezado de la tarjeta: `Visa` / `Mastercard` / `American Express`…) y detecta el tipo aunque el archivo tenga un nombre genérico (`resumen.pdf` → VISA). Orden de prioridad: **nombre del archivo > contenido pág.1 > fallback Banco**. El scan es solo página 1 a propósito: el cuerpo de los resúmenes trae avisos tipo "Visa Airport Companion" que generaban falsos positivos (el scan full-text de `MASTERCARD.pdf` devolvía VISA). CSV/XLSX siguen filename-only. Verificado con suite Deno (17 casos) + matriz e2e 6/6 contra la función desplegada (PDFs genéricos → marca correcta, nombre gana a contenido, período siempre de las fechas).
- **Fase 0 de `improvements.md` — baseline verificado** — antes de arrancar la migración a Vite (Fase 1) se verificó que el stack funciona: **frontend** `CI=true npm test` → 14 suites / **149 tests verdes** y `npm run build` → OK (salida en `build/`); **backend** `deno test` en `functions/` → **26 tests verdes** (17 detection + 6 byma + 3 pool). **Hallazgo**: el `node_modules` del frontend estaba **stale** (la sesión abortada de la migración lo dejó sin `web-vitals`); se resolvió con `npm ci`. Tag de rollback **`pre-fase1`** creado sobre `13c60c6`. Sección FASE 0 en `improvements.md` con los checks `[x]`.
- **Sanitización de nombres de archivo al subir** — Supabase Storage rechaza con `400 InvalidKey` los object keys con caracteres no-ASCII (acentos, ñ, espacios…), así que archivos tipo `Nación.csv` no subían. Nuevo `lib/sanitizeFileName.js` (`sanitizeFileName`, `sanitizeStoragePath`, `uniqueStoragePath`): normaliza NFD (quita diacríticos), reemplaza lo no `[A-Za-z0-9._-]` por `_` (colapsando repetidos y recortando puntas), conserva la extensión y usa fallback `resumen.<ext>`. `UploadSummaries.handleUpload` ahora sube a un path **sanitizado** (y deduplicado con sufijo `_1`/`_2` contra los archivos existentes) pero guarda `file_name` **original** en `card_summaries` (el display y la detección de `parse-summary` siguen viendo `Nación.csv`); si hubo dedupe, el toast de éxito avisa "subido como `Nacion_1.csv` (ya existía un archivo con ese nombre)". Errores de Storage mapeados a mensajes amigables en español (invalid key → "caracteres no permitidos", duplicado → "ya existe"). Sin cambios en backend (`parse-summary` ya normaliza NFD). Tests: `sanitizeFileName.test.js` (9 casos) + 2 casos nuevos en `UploadSummaries.test.js` (sanitiza y conserva original; dedupe con toast) → 133/133.

- **Fase 1 de `improvements.md` — migración CRA → Vite + Vitest** — se eliminaron `react-scripts`, `web-vitals` y `@testing-library/dom`; se agregaron `vite` 8, `@vitejs/plugin-react` 6, `vitest` 4, `jsdom` 30 y `@testing-library/user-event` 14 (devDeps). `package.json` reescrito (scripts `start` = concurrently tailwind+vite, `build` = build:css+vite build, `test` = vitest run); sin `overrides` ni `eslintConfig` de CRA. **Vulnerabilidades: 28 → 0**. `vite.config.js` nuevo con plugin react + server :3000 + bloque test jsdom. `index.html` movido de `public/` a la raíz (`%PUBLIC_URL%`→`/`, `<script type="module">`, tema inline conservado). Env vars `REACT_APP_*`→`VITE_*` (`.env`, `.env.example`, `supabaseClient.js` con `import.meta.env`, `Sidebar.js` logo → `/logo.png`). Los 14 `.test.js` migrados a Vitest (vi.*, `require`→import con mock hoisted, mock global de chartjs y localStorage en `setupTests.js`). `vercel.json` (framework vite, outputDirectory dist), `.gitignore` + `/dist`, README de frontend reescrito. **Hallazgos de la migración**: (1) el oxc de Vite 8 excluye `.js` — se agregó el plugin `transform-jsx-in-js` (`lang: 'jsx'`) en `vite.config.js`; (2) `localStorage` undefined en Node 26 (global experimental) — mock in-memory en setupTests; (3) jsdom 30 computa accnames inline sin espacio — 10 matchers de tests ajustados a `\s*`; (4) OAuth `redirectTo` — url jsdom fijada a `http://localhost`; (5) se borraron huérfanos `reportWebVitals.js`/`logo.svg` y un `build/` legacy. **Verificado**: `npm test` → 14 archivos / **149 tests verdes**, `npm run build` → `dist/` (210 kB gzip vs 219 baseline), `npm start` → :3000 OK, `npm ls keyv flat-cache file-entry-cache` vacío y lockfile sin deps de compromised.md. Sección FASE 1 en `improvements.md` con checks `[x]` + hallazgos.
- **Fase 2 de `improvements.md` — seguridad y secretos (código + deploy + migración)** — commit `716e30a` (tag `pre-fase2`). **Borrados**: `backend/src/supabaseClient.js` (cliente huérfano con service role), `backend/package*.json`, `backend/node_modules/`, y **`backend/.env`** (service role key fuera de disco — ver nota abajo). `backend/.env.example` ahora documenta `SUPABASE_ANON_KEY` (sin service role). **`config.toml` endurecido**: password ≥8 + `letters_digits` (letras+números, elegido por el usuario), `enable_confirmations`, `secure_password_change` (pide la password actual al cambiarla — activado en dashboard por el usuario), `max_frequency 10s`, `verify_jwt` en las 3 funciones, `[db.seed] enabled=false`, apagados `realtime`/`storage.s3_protocol`/`storage.vector`/`analytics`. **Edge Functions**: `_shared/cors.ts` compartido (allowlist `mandarina-fi.vercel.app` + `localhost:3000` + `*.vercel.app`, `Vary: Origin`); `quotes` con cache LRU cap 1000 + `normalizeSymbols` (dedupe, máx 50, sin MEP/CCL) + AbortSignal/timeouts en BYMA/dolarapi; `import-plan` con tope de `file_base64` (~5MB), reemplazo **atómico vía RPC** `replace_user_plan` (migration `0013`, SECURITY INVOKER con guard `auth.uid()`), `parseQuantity` robusto (separador decimal ambiguo) y mensajes genéricos; `parse-summary` con validación UUID, delete previo por `summary_id` (idempotencia de re-parse), mensajes genéricos + `console.error`, signo de PDF preservado y `setStatus` con try/catch. **`_shared/categorize.ts`**: `telecom` → Servicios (alineado con migración 0009) + `categorize_test.ts`. **Frontend**: `.eq('user_id', …)` en los 4 fetches (transactions, portfolio_plan ×2, card_summaries) y ~18 renders de `error.message` → mensajes amigables con `console.error`. **Verificado**: frontend 149/149 + build OK; backend `deno test` 28/28; deploy de las 3 funciones (parse-summary v17, quotes v7, import-plan v4 — ACTIVE); `supabase db push` (0013 aplicada en remoto); `supabase db reset` local con las 13 migraciones (0001–0013) sin errores y seed que ya no rompe; hosting: request sin JWT → **401** (`verify_jwt` activo), CORS refleja solo orígenes permitidos con `Vary: Origin`. **Hallazgo de rotación**: Supabase **ya no permite rotar las legacy keys** (`anon`/`service_role`, solo ver/copiar) — la service role key nunca estuvo comprometida (no entró al historial git), así que se **borró `backend/.env`** (única copia local, gitignored) en vez de rotarla; la vía de rotación moderna (nuevas API keys `sb_publishable`/`sb_secret`, deprecación de legacy a fines 2026) quedó anotada en `TODO.md`. **Pendiente manual del usuario**: solo falta **verificar el signup con confirmación de email** (los settings de auth ya los aplicó en el dashboard).
- **Fase 2 cerrada + Auth UX en español + fix del bug de "No se pudieron cargar los gastos"** — el usuario **verificó en hosting (2026-08-10)** el signup con confirmación de email, la pantalla "Ya existe una cuenta" (email ya registrado) y el email de recuperación → Fase 2 de `improvements.md` marcada **HECHA**. **Auth UX (commit `03f8986`)**: nuevo `lib/authErrors.js` (`authErrorToSpanish` traduce errores de Supabase por `error.code` con fallback por texto: `invalid_credentials`, `weak_password`, `email_not_confirmed`, `user_already_exists`, rate limits); `Auth.js` valida password `letters_digits` en cliente, traduce todos los `setError`, y distingue email existente por `identities: []` **o** `error.code === 'user_already_exists'` → pantalla "Ya existe una cuenta" con botones **Volver a iniciar sesión** y **Recuperar contraseña** (`resetPasswordForEmail`); link "¿Olvidaste tu contraseña?" en el login normal. Tests 160/160. **Fix de bug en Costos (Dashboard)**: el fetch de `transactions` filtraba `.eq('user_id', …)` pero esa tabla **no tiene columna `user_id`** (el ownership se resuelve por RLS vía `card_summaries.summary_id`) → PostgREST devolvía error y la UI mostraba "No se pudieron cargar los gastos". Se quitó el filtro (el único fetch afectado; `card_summaries`/`portfolio_plan` sí tienen `user_id`) + test de regresión que verifica que `transactions` no se filtra por `user_id`.
- **Fase 3 de `improvements.md` — refactor del frontend: duplicación y god components CERRADA** — (commits `60ccfa8`…`a806c58`, 2026-08-10). **Compartido**: hook `usePortfolioQuotes.js` (unifica `loadPlan`/quotes/`builtItems`/`sortedItems`/`refreshQuotes`/sort persistente entre InvestmentPlan y MarketQuotes); `lib/format.js` (`fmt`/`fmtPct`/`fmtCompact` + `fileOf` movido de Dashboard/TransactionsTable), `lib/constants.js` (`ASSET_TYPES`, `MONTHS`, `PALETTE`, `BRAND_HEX`/`brandRgba`), `lib/planSort.js` (`SORT_KEYS`). **UI compartida**: `SortableTh.js`, `Check.js` (+`itemBase`/`itemActive`/`itemInactive`), `AssetForm.js`. **God components descompuestos** (todos ≤ ~400 líneas): `Dashboard` 914→**405** (`SummaryCards` —cards+countup+skeleton—, `SpendingCharts`, `TransactionsTable`, `FiltersBar`), `InvestmentPlan` 816→**399** (`PlanTable` —tabla + filas de edición con AssetForm—, `DistributionPanel` —"Tengo para comprar"—), `MarketQuotes` 629→**323** (`QuotesTable` —asignación doughnut + tabla + PriceChart inline—, `QuoteModal`), `UploadSummaries` 532→**332** (`SummaryItem` —fila renombrar/borrar/clasificar—, `MetaForm`). **Código muerto (3.5)**: `lib/chartjs.js` (1 solo `ChartJS.register`), `hooks/useTheme.js`, `SORT_DEFAULTS` fuera del render, eliminado el dir huérfano `frontend/frontend/` (solo `.vercel`); `reportWebVitals.js`/`logo.svg` ya no existían. No se tocaron `SORT_DIRS` ni el prop `session` de MarketQuotes (las notas de 3.5 sobre ellos quedaron obsoletas: ambos se usan). **Verificado**: `rg` → 1 sola definición de `fmt`/`fileOf`/`ASSET_TYPES`/`PALETTE`/`SortableTh`/`Check`; `npm test` **167/167**; `npm run build` OK. **Docs**: borrado `CONTEXT.md` (redundante con AGENTS.md, movido a `/tmp/opencode/`); `AGENTS_TEAM.md` se mantiene como blueprint de subagentes (sin `.opencode/agent/*.md` todavía).
- **Fase 4 de `improvements.md` — confiabilidad del frontend HECHA** — (2026-08-11). Sin unhandled rejections ni splash infinito. **App.js**: `.catch()` en `getSession().then()` (fallback sesión nula → Auth) + `handleSignOut` con try/catch y toast de error (mantiene la sesión si falla; el logout con éxito sigue con "¡Nos vemos!" verde). **Nuevo `hooks/useAsync.js`** (`{ data, setData, loading, error, reload }`, cancelación en unmount) estandariza el patrón loading/error/success; consumido por los loads de `Dashboard` (transacciones + overrides/custom_categories), `InvestmentPlan`, `MarketQuotes` y `UploadSummaries`. Los fetch fallidos (rechazo de red o `error` de Supabase) re-lanzan siempre el **mensaje amigable** (try/catch interno) y `Dashboard`/`InvestmentPlan`/`MarketQuotes`/`UploadSummaries` ya **muestran ese error en pantalla** en vez de quedar colgados. **Mutaciones con try/catch y toasts de error**: `InvestmentPlan` (guardar activo/borrar/aplicar compra), `Dashboard` (cambiar categoría/crear categoría), `UploadSummaries` (los 4 ya tenían toasts de Fase 2; se validó que ninguno quedó sin toast), `Auth` (login/signup/reset/Google con `authErrorToSpanish`; el flujo base ya existía de Fase 2 — se completaron los `finally` de `setSubmitting`). **Año dinámico**: hardcode `2026` eliminado de `UploadSummaries` (`new Date().getFullYear()`; `MetaForm` ya era dinámico). **Cotizaciones no silenciosas**: `usePortfolioQuotes` expone `quotesError` (fetch inicial y refresh fallidos) → nuevo componente `QuotesErrorNotice` ("Sin conexión") junto al botón refrescar en Plan y Cotizaciones; se limpia automáticamente al recuperar (el refresh mantiene su toast "Precios actualizados"); error en el refresh con toast específico. **Verificado**: grep `.then(` → 4 usos (App, usePortfolioQuotes ×2, MarketQuotes) todos con `.catch`; `npm test` **183/183** (18 suites, +15 tests: `useAsync.test.js`, `usePortfolioQuotes.test.js`, rechazos de getSession/signOut en App, rechazos de loads en Dashboard/InvestmentPlan/MarketQuotes/UploadSummaries, toast de guardado fallido y "Sin conexión" aparece/desaparece en InvestmentPlan/MarketQuotes); `npm run build` OK. (ESLint sigue pendiente — Fase 7; `npm run lint` falla porque eslint no está instalado.)
- **Fase 7 de `improvements.md` — calidad y CI (lint + pre-commit + GitHub Actions + dependabot) CERRADA** — (2026-08-12). **Lint**: frontend ESLint estricto (flat config `eslint.config.js`, JSX en `.js` vía `ecmaFeatures.jsx`, @eslint/js + eslint-plugin-react + react-hooks, globals browser/Vitest) pagando los **325 issues históricos → 0**; backend con `deno lint` nativo (sin typescript-eslint — descartado por exigir node_modules en repo Deno) y el import map a claves bare (`jsr:`/`https://` → `@std/*`, `xlsx`, `unpdf` en `functions/deno.json`). **Formato**: frontend `prettier --check` ya pasaba (baseline no hizo falta); backend **baseline `deno fmt` en `b0416d0`** con `"fmt": { "singleQuote": true, "semiColons": false }` en `functions/deno.json` (formato **flat**, el `options` anidado quedó deprecado en Deno 2.9) para **igualar el estilo del frontend** — el baseline cambió solo wrapping/indent/EOF, sin churn de comillas (18 archivos). `.prettierrc.json` (+`.prettierignore`) agregados. **husky + lint-staged** (`d3e0717`): `prepare: "cd .. && husky frontend/.husky"` (husky 9 exige `.git` en el cwd literal, por eso se inicializa desde la raíz); `lint-staged` en `frontend/package.json` (`*.{js,jsx}` → `eslint --fix` + `prettier --write`); `frontend/.husky/pre-commit` corre `npx lint-staged` (previo `git rev-parse --show-toplevel` para resolver el root) y, si hay `.ts` del backend staged, corre `deno fmt --check` + `deno lint` sobre todo `backend/supabase/functions` (rápido, 20 archivos). `"type": "module"` en el package.json del frontend (quita el warning `MODULE_TYPELESS_PACKAGE_JSON` en el hook; stack ya 100% ESM). **Pre-commit verificado en ambos caminos**: bloquea un archivo con `no-unused-vars` y deja pasar un archivo limpio (prettier reformatea). **CI** (`.github/workflows/frontend.yml` + `backend.yml`): push/PR a master → frontend `npm ci` → `npm run lint` → `npm run build` → `npm test` → `npm audit --audit-level=high` (Node 22 LTS) y backend `deno fmt --check` → `deno lint` → `deno test` (deno 2.x); sin secrets ni deploy (Vercel autodeploya desde master). **Dependabot** (`.github/dependabot.yml`): npm `/frontend` + github-actions `/`, semanal. **Auditoría**: `npm audit --audit-level=high` → 0 vulnerabilidades. **Docs**: sección **Convención de commits** y comandos reales en `AGENTS.md`; `improvements.md` corregido (7.1 flat config, no `.eslintrc`). Verificado: `deno fmt --check`/`deno lint`/`deno test` (64/64)/`deno check` OK en las 3; `npm test` 183/183; pre-commit bloquea lint errors.
- **Fase 6 de `improvements.md` — tests: mocks compartidos y cobertura de huecos CERRADA** — (2026-08-12). **Mock de supabase único**: nuevo `src/test/setup.js` con `createSupabaseMock()` (auth/from/storage/functions) registrado **una sola vez** como mock global en `src/setupTests.js` (setupFiles) — antes el `vi.mock` del cliente se repetía en 7 test files. API: `mockTable(table, rowsOrConfig?, error?)` (cadenas cacheadas por tabla; `{ data: rows }` para select→eq→order y modos insert/update/delete/upsert con `{ data: insert, error: insertError }` / `{ data: null, error: <modo>Error }`), `tableChain(table)` para asertar (los métodos son vi.fn), factories `wrap()/tx()/summary()/planItem()`. Refactorizados a `mockTable`/`tableChain`: `App`, `Auth`, `Dashboard`, `InvestmentPlan`, `MarketQuotes`, `UploadSummaries` (el más complejo — `.insert().select().single()`, update/delete `.eq()`) y `usePortfolioQuotes`; `vi.clearAllMocks()` en `beforeEach`. **Tests de huecos**: cubiertos `Toast` (auto-dismiss con fake timers, estilos por tipo, iconos wave/none), `Dropdown` (portal a body, Escape/click-fuera, búsqueda con data-pinned, render-prop, closeOnSelect, scroll-fuera), `FiltersBar` (período/resumen/categorías/moneda/búsqueda/limpiar-filtros, fechas custom en portal), `PriceChart` (rangos, encabezado ±, loading/error/sin-datos), `Sidebar` (3 items, active, colapsado/expandido, navigate, Logo), `MarketClosedNotice`, y hooks `useCountUp` (rAF, reduced-motion, cancel en unmount) y `useTheme` (sistema, localStorage, migración de claves legadas, toggle). `usePortfolioQuotes` ampliado de 2 a 8 tests (rates MEP/CCL, scaling ARS↔USD, mercado cerrado, catch de refresh). **Coverage**: `@vitest/coverage-v8` instalado, script `npm run coverage`, `vite.config.js` con `include: ['src/lib/**', 'src/hooks/**']`, `exclude: ['src/lib/supabaseClient.js']` (siempre mocked, sin lógica) y thresholds globales ≥80; `coverage/` agregado a los ignores de ESLint. **Verificado**: **230 tests / 26 files verdes** (era 183/18 al iniciar); coverage real **statements 97.77, branches 89.33, functions 97.67, lines 98.85** (meta ≥80); `npm run lint` limpio, prettier limpio, `npm run build` OK, `npm audit` 0. Docs actualizados (improvements Fase 6, AGENTS.md comando `coverage` + estructura, HANDOFF).
