# TODO Mandarina

Lista viva del proyecto. Se actualiza en cada iteración. Lo completado se archiva en `DONE.md`.

## 🔄 En progreso

- **Orden de la cola (decisión del usuario 2026-08-13)**: 1) QW-C Ingresos → **HECHO** 2) QW-D layout/toggle → **HECHO** 3) QW-E cards 4 simétricas → **HECHO** 4) QW-F cambio de contraseña → **HECHO** 5) QW-G drawer móvil → **HECHO** 6) QW-H fix botón duplicado + números → **HECHO** (ver DONE). **Backend todo al final** (últimos items del roadmap).
- **Monetización: idea C (Reportes Pro) en producción** — ver item del roadmap abajo. Siguiente en la cola de monetización (sin fecha): **paso 2 del billing MercadoPago**.

## 📋 Pendiente (roadmap)

### Frontend-only (orden sugerido de ejecución)

- **Agregar idioma inglés a la página (con selector) — HECHO (2026-08-15, Fase 1 + Fase 2 completas, ver DONE.md)** — toggle ES|EN en landing y dashboard; detección de navegador + localStorage; dashboard completo ES/EN. Detalle en `DONE.md` y plan en `fase9.md`.

- **Ingresos: vista de detalle propia (QW-C) — HECHO (2026-08-13, ver DONE.md)** — pestaña Ingresos con subtotales de acreditaciones + análisis de recurrencia (sueldo vs devoluciones) + badge "Recurrente". Sin backend.

- **Móvil (`<lg`) — drawer HECHO (2026-08-13, ver DONE.md)** — navegación móvil con drawer deslizante custom (branding, items coherentes con el rail, email + Cerrar sesión, cierre X/backdrop/Escape). Edge case documentado: el tour no resalta los items de nav en mobile con el drawer cerrado (saltea targets sin tamaño visible).

- **GEO SEO + AEO — HECHO / CERRADO (2026-08-15, ver DONE.md)** — 3 iteraciones: (1) landing pública pre-auth + `index.html` con OG/canonical/JSON-LD `FAQPage` + `robots.txt` con AI crawlers + `sitemap.xml` + `llms.txt`; (2) páginas estáticas `/overview` y `/alternatives` + JSON-LD `WebSite`/`FinancialService` (`areaServed: Argentina`, `alternateName: "Mandarina Fi"`) + Landing con "Cómo funciona"/"¿Cuándo conviene?"/"Quién la usa" (testimonio real); (3) cierre. Los re-audits de llmaudit dieron **22 → 16 → 15** → on-page "agotado"; gap = autoridad off-page. **Actualización (2026-08-16)**: el usuario recibió un **mail de llmaudit con score 61/100** → el on-page ahora rinde (los modelos ya leen/recomiendan el sitio). **Pendiente del usuario (sin fecha)**: crear los perfiles externos de `offpage.md` (repo público, AlternativeTo, Product Hunt, directorios) para sumar `sameAs` al JSON-LD y re-correr llmaudit en 4-8 semanas. Fuera de alcance por decisión del usuario: dominio custom (100% gratis) y contenido educativo (EN HOLD).

- **Evaluar D3** — si el dashboard necesita visualizaciones custom que Chart.js no cubra bien, migrar/escalar a D3 (anotado; por ahora Chart.js alcanza).

- **Memoria de opencode — capas 1+2+DECISIONS hechas** — hoy están `AGENTS.md` + `HANDOFF.md` (auto-cargadas vía `.opencode/opencode.json`) + `DECISIONS.md` (log de decisiones, ver raíz). Cuando surja necesidad: **skills** on-demand para procedimientos repetidos (deploy, migraciones) y **subagentes** de dominio (frontend/backend/reviewer). El agente debe señalar cuándo valen la pena.

- **Monetización — idea C (Reportes Pro) EN PRODUCCIÓN, billing MP deferido (2026-08-19, ver `DECISIONS.md`)** — la app era 100% gratis; **rumbo decidido**: monetizar la **exportación avanzada (C)** como primer feature del tier Pro, con el resto gratis. Los reportes quedaron **completos detrás del paywall** (gating en Sidebar/MobileDrawer + branch de `App.js` + `ProProvider`/`usePro` + tabla `subscriptions` migración `0023`, activación manual por SQL editor). Las otras ideas de `monetization.md` (Mandi Pro, límite de resúmenes, datos extra en cotizaciones, afiliados, donación) quedan anotadas sin prioridad. **PASO 2 (futuro) — billing MercadoPago**: edge `mp-checkout` (crea preapproval con `external_reference = user_id`, devuelve `init_point`), edge `mp-webhook` (`verify_jwt = false`, firma HMAC `x-signature` con secret `MERCADOPAGO_WEBHOOK_SECRET` — primer secret custom del proyecto) → RPC `set_subscription_status` `security definer` (único punto de escritura server-to-server; primer uso de la secret key, scoped a un RPC, registrar en DECISIONS), pricing final ARS + testing sandbox.

- **Mejorar versión mobile + app en App Store** — además del item "Móvil (`<lg`)" (drawer/bottom nav), evaluar empaquetar la app como **PWA instalable** y/o app nativa (Capacitor) para publicar en App Store. Requiere cuenta de desarrollador Apple ($99/año) — choca con la regla "100% gratis"; decidir si el usuario paga o se queda en PWA.

- **Infra del rebrand — HECHO gratis (sin dominio propio)** — "fimplify" queda solo en fallbacks intencionales de localStorage. La **compra de `mandarina.app` se descartó** (siempre paga, ~US$12-15/año; el usuario prefiere 100% gratis pero prolijo). Lo realizado: proyecto Vercel renombrado `fimplify` → **`mandarina`** (URL principal **`mandarina-fi.vercel.app`**, porque `mandarina.vercel.app` ya está tomado por otro equipo; los dominios `fimplify`/`mandarina-fin`/`mandarina-finanzas` se borraron) y repo GitHub renombrado a **`alvarezem/mandarina`** (remote local actualizado). **Deploy**: antes por CLI (`vercel --prod`); hoy **autodeploy desde GitHub** (rama `master`, solo producción) — ver DONE. Si algún día se quiere marca limpia, el único paso pendiente sería **comprar `mandarina.app`** en Cloudflare y agregarlo como dominio custom en Vercel.

### Backend / deferido (anotado al final, según decisión del usuario)

- **Área Inversiones — Plan, Cotizaciones, gráficos, watchlist y ledger HECHOS (ver DONE.md)** — 3 sub-menús (tabs): **Plan de inversión** (meta % vs actual %, importar XLSX + edición manual, precios live vía proxy `quotes`, toggle ARS↔USD con CCL/MEP, presupuesto de compra con estrategias de prioridad — el botón comprar además registra la operación en el ledger), **Cotizaciones en vivo** (patrimonio total con Δ diaria ponderada, cards MEP/CCL, doughnut de asignación, tabla live, gráfico de precio por activo con rangos 1S/1M/3M/1A, franja de sesión del día, en la moneda del toggle) y **Operaciones (ledger)** (registro compras/ventas/ajustes, costo promedio móvil con comisiones, rentabilidad vs. costo con precios live, alta en pop-up con nota grande y BYMA siempre visible, comisión en $ o % con toggle, nota visible como columna, campos obligatorios; alimenta a Mandi a futuro). Debajo de cotizaciones, la sección **"Mi watchlist"** (tabla DB `watchlist` con RLS own, alta/baja con confirmación, precios live + change% ▲▼ y sigue el toggle ARS/USD). El **orden de las tablas es compartido y persistente** (default % Meta mayor→menor, se cambia desde el Plan, se recuerda entre sesiones). Toasts de saludo al login y variación de las **top 3 posiciones**. La fuente de precios de la edge `quotes` pasó de **Yahoo Finance (bloqueado con 429) a BYMA Open Data** (`open.bymadata.com.ar`, oficial y gratis, ~20 min de demora) manteniendo dolarapi para MEP/CCL; `import-plan` ahora hace **rollback** si el insert falla y expone el error real.

- **Asistente IA "Mandi"** — integrar un asistente que responda preguntas sobre el consumo usando el análisis de resúmenes (se alimenta de la tabla `ledger_operations` del Plan — ya creada en `0016_ledger.sql` — para "¿cuánto ganaste con GGAL?").

- ⏸️ **Inversiones: ONs/cauciones — EN HOLD (2026-08-15, decisión del usuario)** — ampliar `ASSET_TYPES` en el Plan y Cotizaciones con **ONs** (símbolo `+`), cauciones y demás instrumentos de contado. **Verificado (2026-08-15)**: BYMA Open Data ya devuelve ONs con `settlementType:'2'` (TLCTO/VSCRO del plan del usuario parsean sin cambios en la edge) y la columna `asset_type` es texto libre (sin migración DB). **Motivo del hold**: de los 5 tickers del usuario (`PN360, TLCTO, TTC90, VSCRO, YM340`), **PN360/TTC90/YM340 no existen en el feed gratis** (ni cotización con st 1/2/3/6/7 ni histórico) y las cauciones con símbolos estándar (`A7D`, `A30D`, `60D`…) tampoco resuelven → agregar los tipos solo mostraría **datos vacíos**. **Decisión: no agregar cosas que muestren datos vacíos.** Reactivar cuando el usuario confirme tickers que BYMA free publique (o si se suma otra fuente). Los **futuros/opciones** (Matba Rofex) quedan anotados como fase aparte: no están en el feed de BYMA Open Data y la data de derivados suele ser de pago — decidir fuente (o arrancar sin ellos). Decisión del usuario: **no** crear tab separado por ahora.

- 🔴 **Migrar a las nuevas API keys de Supabase (`sb_publishable_…`/`sb_secret_…`) — HECHO (2026-08-14, ver DONE.md)** — publishable reemplaza `anon` (client-side); secret reemplaza `service_role` pero **no se usa** (las Edge Functions autentican con el JWT del usuario). Frontend + Vercel con la publishable; funciones migradas a `_shared/supabase.ts` (`createUserClient` lee `SUPABASE_PUBLISHABLE_KEYS`, sin fallback a legacy) y deployadas. **Las legacy keys ya las desactivó el usuario (2026-08-15)** → item cerrado.

## ⏸️ En hold (refactor / limpieza — posponer hasta estabilizar)

- **Contenido educativo (guías de finanzas personales en Argentina) — EN HOLD (2026-08-15, decisión del usuario)** — el reporte de llmaudit (iteración 2) sugiere "desarrollar contenido educativo sobre cómo elegir servicios de finanzas personales" para ganar autoridad. El usuario **no está capacitado para crearlo** → se descarta por ahora. Reactivar si algún día se quiere un blog/sección de guías (requeriría definir autor/calidad).
- **OAuth de Google — dev local con `supabase start` (Opción B)** — el flujo **Google OAuth ya está configurado y verificado** en producción (provider activado, credenciales en Google Console, redirects OK para `mandarina-fi.vercel.app` y `localhost:3000`, auto-linkeo por email confirmado — ver DONE). Hoy el frontend local apunta al Supabase **hosted** (`frontend/.env` → el proyecto hosted), sin tocar `config.toml`. Lo único en hold es aislar el desarrollo en una instancia local completa, que requeriría: `[auth.external.google]` en `backend/supabase/config.toml` (`enabled = true`, `client_id`/`secret` con `env(...)`) + vars en `backend/.env`, y `http://localhost:3000/auth/v1/callback` en las redirect URIs de Google Console y el allow-list local. En hold por decisión del usuario: no es necesario por ahora.
- 🔴 **IMPORTANTE — Mapeo de columnas/formatos (nuevos bancos)** — el parser heurístico de CSV/XLSX (`HEADER_ALIASES`, `findColumns`) y el posicional x,y del PDF (layout fijo BBVA) están afinados solo para las muestras actuales (MercadoPago CSV + BBVA Visa/Mastercard). Cuando se agreguen más bancos habrá que: ampliar aliases (cada banco usa otros nombres/orden, a veces varios montos: débito/crédito, PESOS/DÓLARES, saldo), elegir bien la columna de monto y **generalizar el parser de PDF** (auto-detectar columnas por cluster de coordenadas x o por header). En hold por decisión del usuario: por ahora no agrega más bancos.
- **Conversión PDF→Markdown (Microsoft MarkItDown)** — **EVALUADO → NO recomendado** para estos casos. Motivo: su conversión de PDF por defecto (`pdfminer.six`) pierde estructura/tablas y rinde mal en tablas multi-columna densas; los resúmenes de banco son justamente tablas posicionales con columnas PESOS/DÓLARES que el parser actual (`unpdf` + x,y) ya resuelve bien. Además descarta las coordenadas x,y (señal clave para separar columnas) y es Python (no corre en Deno; REST = dependencia externa, latencia y enviar data financiera a un tercero). El camino correcto es el de arriba: generalizar el parser posicional, no convertir a Markdown.
- **Consolidar `DONE.md` / `HANDOFF.md` / `TODO.md` — HECHO (2026-08-16, ver `DECISIONS.md`)** — se definió la fuente única por tipo de info (historial completo → `DONE.md`, handoff corto y accionable → `HANDOFF.md`, roadmap/status → `TODO.md`, decisiones → `DECISIONS.md`) y se deduplicó: `HANDOFF.md` corto, `TODO.md` con HECHO a una línea y los 3 GEO colapsados en 1.
- `parse-summary` — el archivo concentra parseo + categorías + análisis; modularizar cuando la feature-set se estabilice.