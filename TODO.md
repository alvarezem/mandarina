# TODO Mandarina

Lista viva del proyecto. Se actualiza en cada iteración. Lo completado se archiva en `DONE.md`.

## 🔄 En progreso

- **Backend / diferido** — primer item de la cola (**watchlist**) **HECHO (2026-08-14, ver DONE.md)**; segundo item (**ledger del Plan**) **HECHO (2026-08-14, ver DONE.md)**; **migrar a las nuevas API keys de Supabase → HECHO (2026-08-14, ver el item 🔴 abajo)**. Siguiente en orden era **ONs/cauciones en la edge `quotes` → EN HOLD (2026-08-15, ver item abajo)**. Cierre de la sesión 2026-08-15: **fix móvil del módulo Operaciones** (recuadros del resumen apilados en <640px) — ver `DONE.md`. Receta en `HANDOFF.md`.
- **Orden de la cola (decisión del usuario 2026-08-13)**: 1) **QW-C Ingresos → HECHO** 2) **QW-D layout/toggle → HECHO** 3) **QW-E cards 4 simétricas → HECHO** 4) **QW-F cambio de contraseña → HECHO** 5) **QW-G drawer móvil → HECHO** 6) **QW-H fix botón duplicado + números → HECHO** (ver DONE). **Backend todo al final** (últimos items del roadmap).

## 📋 Pendiente (roadmap)

### Frontend-only (orden sugerido de ejecución)

- **Ingresos: vista de detalle propia (QW-C) — HECHO (2026-08-13, ver DONE.md)** — la pestaña **Ingresos** ya cubría lo esencial (cards, totales ARS/USD, mayor ingreso, desglose por categoría/origen y tendencia de créditos). **Completado**: tabla con subtotales de acreditaciones + análisis de recurrencia (**sueldo vs devoluciones**) vía `buildIncomeSources` en `buildIncomeAnalysis` (mismo merchant en ≥2 meses distintos del joined `card_summaries.period_month`, umbral decidido con el usuario) y sección compacta en modo ingresos con badge "Recurrente". Sin backend.

- **Móvil (`<lg`) — drawer HECHO (2026-08-13, ver DONE.md)** — la navegación móvil ya no usa la bottom nav: ahora un **drawer deslizante custom** desde la hamburguesa del header móvil, con branding (logo, blur de backdrop, animación `slide-in-left`), items con iconos coherentes con el rail desktop, email + Cerrar sesión en el footer, cierre con X/backdrop/Escape y cierre automático al navegar. Queda anotado como edge case que el tour no resalta los ítems de nav en mobile con el drawer cerrado (el tour saltea targets sin tamaño visible).

- **GEO SEO + AEO (llmaudit.app score 22, "likely invisible")** — la app es una SPA 100% login-gated (sin sesión solo renderiza Auth), así que para los bots/answer engines la superficie pública es solo `index.html` (meta description básica, sin OG/canonical/JSON-LD) + `robots.txt` minimalista. El reporte de llmaudit pide responder en **contenido público** las preguntas: **"why choose mandarina fi"**, **"what is mandarina fi and who is it for"** y **"what to expect"**. Esquema sugerido (a confirmar en esa sesión): (1) `index.html` con title/description mejorados + Open Graph/Twitter/canonical + **JSON-LD** (`Organization`/`SoftwareApplication`/`FAQPage`); (2) **landing pública** que responda las 3 preguntas + FAQ — hoy no existe, todo está tras login (decidir: render pre-auth en la SPA vs. página estática en Vercel); (3) `robots.txt` con reglas para AI crawlers (GPTBot, OAI-SearchBot, ClaudeBot, PerplexityBot) + `sitemap.xml` + opcional `llms.txt`. Re-correr llmaudit post-deploy para verificar la mejora.

- **Evaluar D3** — si el dashboard necesita visualizaciones custom que Chart.js no cubra bien, migrar/escalar a D3 (anotado; por ahora Chart.js alcanza).

- **Memoria de opencode — evaluar capas 3-5** — hoy están las capas 1+2 (`AGENTS.md` + `HANDOFF.md`, auto-cargadas vía `.opencode/opencode.json`). Cuando surja necesidad, implementar: **skills** on-demand para procedimientos repetidos (deploy, migraciones), **subagentes** de dominio (frontend/backend/reviewer), y **DECISIONS.md** (ADR ligeros). El agente debe señalar cuándo valen la pena.

- **Pensar ideas de suscripción por servicio** — la app es 100% gratis hoy; explorar un tier pago para monetizar: Mandi (asistente IA), límites de resúmenes/mes, reportes avanzados/exportación, más instrumentos en cotizaciones, versión premium sin fallbacks. Listar ideas con tradeoffs (límites del free tier de Supabase, billing vía Stripe/MercadoPago) antes de decidir. Sin fecha.

- **Mejorar versión mobile + app en App Store** — además del item "Móvil (`<lg`)" (drawer/bottom nav), evaluar empaquetar la app como **PWA instalable** y/o app nativa (Capacitor) para publicar en App Store. Requiere cuenta de desarrollador Apple ($99/año) — choca con la regla "100% gratis"; decidir si el usuario paga o se queda en PWA.

- **Infra del rebrand — HECHO gratis (sin dominio propio)** — "fimplify" queda solo en fallbacks intencionales de localStorage. La **compra de `mandarina.app` se descartó** (siempre paga, ~US$12-15/año; el usuario prefiere 100% gratis pero prolijo). Lo realizado: proyecto Vercel renombrado `fimplify` → **`mandarina`** (URL principal **`mandarina-fi.vercel.app`**, porque `mandarina.vercel.app` ya está tomado por otro equipo; los dominios `fimplify`/`mandarina-fin`/`mandarina-finanzas` se borraron) y repo GitHub renombrado a **`alvarezem/mandarina`** (remote local actualizado). **Deploy**: antes por CLI (`vercel --prod`); hoy **autodeploy desde GitHub** (rama `master`, solo producción) — ver DONE. Si algún día se quiere marca limpia, el único paso pendiente sería **comprar `mandarina.app`** en Cloudflare y agregarlo como dominio custom en Vercel.

### Backend / deferido (anotado al final, según decisión del usuario)

- **Área Inversiones — Plan, Cotizaciones, gráficos, watchlist y ledger hechos** — la sección Inversiones ahora tiene **3 sub-menús (tabs)**: **Plan de inversión** (meta % vs actual %, importar XLSX + edición manual, precios live vía proxy `quotes`, toggle ARS↔USD con CCL/MEP, presupuesto de compra con estrategias de prioridad — el botón comprar además registra la operación en el ledger), **Cotizaciones en vivo** (resumen estilo Bull Market: patrimonio total con Δ diaria ponderada, cards MEP/CCL, doughnut de asignación y tabla de cotizaciones live con precio/variación/volumen/valor/% cartera; **gráfico de precio por activo** inline o en modal con rangos 1S/1M/3M/1A vía histórico BYMA, **franja de sesión del día** (apertura/cierre prev/máx/mín/hora) y **en la moneda del toggle**) y **Operaciones (ledger — HECHO 2026-08-14, ver DONE)** (registro de compras/ventas/ajustes, costo promedio móvil con comisiones, rentabilidad vs. costo con precios live, independiente de `portfolio_plan`; alta en **pop-up** con nota grande y **BYMA siempre visible** (fetch on click), comisión en **$ o %** con toggle, **nota visible** como columna, campos obligatorios marcados; alimenta a Mandi a futuro). Debajo de cotizaciones, la sección **"Mi watchlist"** (tabla DB `watchlist` con RLS own, alta/baja con confirmación, precios live + change% ▲▼ y sigue el toggle ARS/USD — **HECHO 2026-08-14**, ver DONE). El **orden de las tablas es compartido y persistente** (default % Meta mayor→menor, se cambia desde el Plan, se recuerda entre sesiones). Toasts de saludo al login ("Bienvenido/a"/"Volviste") y variación de las **top 3 posiciones**. La fuente de precios de la edge `quotes` pasó de **Yahoo Finance (bloqueado con 429) a BYMA Open Data** (`open.bymadata.com.ar`, oficial y gratis, ~20 min de demora) manteniendo dolarapi para MEP/CCL; `import-plan` ahora hace **rollback** si el insert falla y expone el error real.

- **Asistente IA "Mandi"** — integrar un asistente que responda preguntas sobre el consumo usando el análisis de resúmenes (se alimenta de la tabla `ledger_operations` del Plan — ya creada en `0016_ledger.sql` — para "¿cuánto ganaste con GGAL?").

- ⏸️ **Inversiones: ONs/cauciones — EN HOLD (2026-08-15, decisión del usuario)** — ampliar `ASSET_TYPES` en el Plan y Cotizaciones con **ONs** (símbolo `+`), cauciones y demás instrumentos de contado. **Verificado (2026-08-15)**: BYMA Open Data ya devuelve ONs con `settlementType:'2'` (TLCTO/VSCRO del plan del usuario parsean sin cambios en la edge) y la columna `asset_type` es texto libre (sin migración DB). **Motivo del hold**: de los 5 tickers del usuario (`PN360, TLCTO, TTC90, VSCRO, YM340`), **PN360/TTC90/YM340 no existen en el feed gratis** (ni cotización con st 1/2/3/6/7 ni histórico) y las cauciones con símbolos estándar (`A7D`, `A30D`, `60D`…) tampoco resuelven → agregar los tipos solo mostraría **datos vacíos**. **Decisión: no agregar cosas que muestren datos vacíos.** Reactivar cuando el usuario confirme tickers que BYMA free publique (o si se suma otra fuente). Los **futuros/opciones** (Matba Rofex) quedan anotados como fase aparte: no están en el feed de BYMA Open Data y la data de derivados suele ser de pago — decidir fuente (o arrancar sin ellos). Decisión del usuario: **no** crear tab separado por ahora.

- 🔴 **Migrar a las nuevas API keys de Supabase (`sb_publishable_…`/`sb_secret_…`) — HECHO (2026-08-14, ver DONE.md)** — Supabase **depreca las legacy keys** (`anon`/`service_role`) a fines de 2026; hay que migrar antes o la app se rompe. Además, las legacy keys **ya no se pueden rotar** (solo ver/copiar; verificado 2026-08-09) — las nuevas keys sí son revocables individualmente (es la única vía de rotación que queda). La **publishable reemplaza `anon`** (client-side); la **secret reemplaza `service_role`** (server-side) y **no se usa en el proyecto** (las Edge Functions autentican con el JWT del usuario, nunca `service_role` — se guarda solo en el dashboard). **Hecho**: publishable `default` creada en Settings → API Keys; `frontend/.env` + **Vercel** (Production y Preview) con el valor `sb_publishable_…`; Edge Functions migradas a `_shared/supabase.ts` (`createUserClient` lee `SUPABASE_PUBLISHABLE_KEYS`, JSON auto-inyectado, sin fallback a legacy) y **deployadas**; verificado el bundle sin el JWT anon legacy. **Pendiente del usuario**: desactivar las legacy keys (`anon`/`service_role`) en Settings → API Keys (reversible) tras confirmar la app en uso. Referencia: guía oficial "Migrating to new API keys".

## ⏸️ En hold (refactor / limpieza — posponer hasta estabilizar)

- **OAuth de Google — dev local con `supabase start` (Opción B)** — el flujo **Google OAuth ya está configurado y verificado** en producción (provider activado, credenciales en Google Console, redirects OK para `mandarina-fi.vercel.app` y `localhost:3000`, auto-linkeo por email confirmado — ver DONE). Hoy el frontend local apunta al Supabase **hosted** (`frontend/.env` → el proyecto hosted), sin tocar `config.toml`. Lo único en hold es aislar el desarrollo en una instancia local completa, que requeriría: `[auth.external.google]` en `backend/supabase/config.toml` (`enabled = true`, `client_id`/`secret` con `env(...)`) + vars en `backend/.env`, y `http://localhost:3000/auth/v1/callback` en las redirect URIs de Google Console y el allow-list local. En hold por decisión del usuario: no es necesario por ahora.
- 🔴 **IMPORTANTE — Mapeo de columnas/formatos (nuevos bancos)** — el parser heurístico de CSV/XLSX (`HEADER_ALIASES`, `findColumns`) y el posicional x,y del PDF (layout fijo BBVA) están afinados solo para las muestras actuales (MercadoPago CSV + BBVA Visa/Mastercard). Cuando se agreguen más bancos habrá que: ampliar aliases (cada banco usa otros nombres/orden, a veces varios montos: débito/crédito, PESOS/DÓLARES, saldo), elegir bien la columna de monto y **generalizar el parser de PDF** (auto-detectar columnas por cluster de coordenadas x o por header). En hold por decisión del usuario: por ahora no agrega más bancos.
- **Conversión PDF→Markdown (Microsoft MarkItDown)** — **EVALUADO → NO recomendado** para estos casos. Motivo: su conversión de PDF por defecto (`pdfminer.six`) pierde estructura/tablas y rinde mal en tablas multi-columna densas; los resúmenes de banco son justamente tablas posicionales con columnas PESOS/DÓLARES que el parser actual (`unpdf` + x,y) ya resuelve bien. Además descarta las coordenadas x,y (señal clave para separar columnas) y es Python (no corre en Deno; REST = dependencia externa, latencia y enviar data financiera a un tercero). El camino correcto es el de arriba: generalizar el parser posicional, no convertir a Markdown.
- `frontend/src/index.css` — ahora es el entry de Tailwind (imports + `@theme` con tokens de color/animación + keyframes) y `body` base.
- **Consolidar `DONE.md` / `HANDOFF.md` / `TODO.md`** — hay información compartida duplicada en los tres (ej. el estado del ledger/cola aparece en HANDOFF, TODO y las entradas de DONE; las entradas de DONE duplican status que también viven en TODO). Definir la **fuente única de verdad** por tipo de info (historial completo → DONE, handoff corto y accionable → HANDOFF, roadmap/status → TODO) y deduplicar.
- `parse-summary` — el archivo concentra parseo + categorías + análisis; modularizar cuando la feature-set se estabilice.
