# Backend = 100% Edge Functions (Deno) + migraciones. Sin service role key:
# las funciones usan la anon key + JWT del usuario y delegan en RLS.
# En deploy hosted las variables SUPABASE_URL / SUPABASE_ANON_KEY las inyecta
# Supabase automáticamente; acá quedan solo para desarrollo local.
SUPABASE_URL=
SUPABASE_ANON_KEY=
